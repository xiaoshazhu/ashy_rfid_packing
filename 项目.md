# 激光扫码项目当前状态、按钮合并与交付计划

> 更新时间：2026-07-29  
> 本机：用于读取、修改、合并和验证代码。  
> 设备电脑：连接相机、顶部按钮、RS485转换器、打印机等外部硬件并执行现场测试。  
> 状态口径：只把有设备日志或实物结果支持的项目标为“设备验证通过”；代码存在不等于实机通过。

## 2026-07-29 最新增量状态（本节优先于下方早期审计结论）

- 顶部按钮：朋友提供的TD-39前5路监听代码已经合并，现场反馈硬件通信已连通；各业务动作仍需逐项回归。
- 物理灯光：已按项目内WDIP24-15-R Modbus协议接入真实设置电压与回读。校准不再使用固定百分比或相机曝光代替灯光；它会实测当前安全上限，并对亮度区间逐层二分，每档采5帧真实图像，按识别成功率、识别数量和解码耗时选最佳值。最佳值必须在设备电脑放置真实盒子后才能得出。
- 扫码：运行路径已去除识别失败后自动生成6条模拟盒码和强制录入；失败只提示失败。
- 箱码：标签箱码保持相机识别原文，不再改成13位时间戳；本次识别失败或重置后会清除旧预览箱码。
- RFID：真正打印写卡时单独生成13位毫秒时间戳。当前按测试要求保持防二次写入锁关闭（`allow_reprint_same_code=true`）。
- 当前真实扫码入口只打开打印排版预览，不写数据库、不调用打印机、不写RFID。
- 打印/RFID仍未实机验收。下方早期审计指出的“打印失败仍可能更新页面并提示成功”及“RFID未做真实读回一致性判断”仍是生产交付前阻断项。

## 一、当前结论

项目目前**可以启动，主页面、海康相机和屏幕按钮能够运行，但尚不具备生产交付条件**。

当前最直接的阻断项是顶部4个实体按钮尚未连通。朋友如果把按钮通信跑通，只代表“硬件输入层”取得突破，后面仍需要完成代码合并、业务保护、打印机实测、真实装箱扫码回归、网络与数据库一致性验证、测试模拟逻辑清理和正式打包。

当前不能直接作为生产版交付的主要原因：

1. 当前 `RS485Utils.py` 是 WDIP24-15-R **只读诊断模式**；WDIP是可调电源，不是顶部按钮输入模块，不能作为最终按钮实现。
2. 扫码失败时会自动生成6条模拟盒码，并可能强制写入本地数据库。
3. 打印失败后页面仍会提示“打印箱码成功”、保存箱码并允许继续装箱。
4. 打印代码默认允许重复写卡，并且当前RFID“核验成功”判断并没有真实比对TID/EPC。
5. WebSocket连接任务和主程序运行的异步事件循环不一致，历史日志出现过 `Task was destroyed but it is pending`，云端在线上传尚未跑通。
6. 装箱数量、重复盒码、防连击、重新装箱确认等生产约束仍不完整。
7. 当前打包文件是2026-05的旧版本，未包含后来增加的RFID SDK、模板、资源和新页面。

## 二、目前项目已经实现了什么

| 模块 | 当前情况 | 状态 |
|---|---|---|
| 程序启动 | `start.bat` 能在设备电脑选择可用Python并启动源码 | 已有设备启动日志 |
| 主页面 | 主界面、设置、日志、计数、装箱进度、箱码显示等控件已存在 | 屏幕操作可用，仍需业务回归 |
| 海康相机 | 能发现设备、打开设备、开始/停止取流、显示实时画面 | 已有设备日志支持 |
| 二维码识别 | CLAHE和二值化两条路径识别二维码，并在单次结果内去重 | 代码已实现，真实连续生产识别待验证 |
| 本地数据库 | SQLite保存箱码、盒码绑定、待上传记录和历史记录 | 代码已使用；生产数据必须保护 |
| WebSocket | 有连接、心跳、发送、接收echo回执和手动补传代码 | 尚未完成有效设备联调，存在事件循环问题 |
| 屏幕按钮 | 设备复位、确认录入、拍照识别、打印箱码、重新装箱等入口存在 | 屏幕按钮能进入业务函数 |
| 顶部按钮 | 通道到页面按钮的映射框架和Qt主线程投递框架存在 | 实体通信未跑通；当前还是诊断状态 |
| 打印/RFID | 已接入T63R SDK封装、标签绘制、EPC写入、CSV记录框架 | 仅代码存在；当前记录8次全部FAIL，不能写“实机通过” |
| 打印模板页面 | 可编辑字段、筛选、预览、保存模板配置 | 预览是绘制的示意条纹，不等于真实条码或实物打印验证 |
| “亮度”页面 | 可以调节海康相机曝光，并执行5轮识别评分 | 实际控制的是相机曝光，不是独立物理灯具 |
| 诊断工具 | 有RS485只读探针、打印机诊断脚本和RFID测试脚本 | 仅用于开发/验收，不应混入生产操作入口 |

### 已完成的静态检查

- 已只读检查36个主要Python源文件，语法错误为0。
- 当前Git分支为 `master`。
- 当前工作区不是干净状态：5个已跟踪文件被修改，并有多个未跟踪SDK、页面、配置和文档目录；生成本文档前Git状态共30项（不含本文档本身）。
- 因此朋友代码到来前必须先做代码快照，不能直接整目录覆盖。

### 当前数据库情况（仅指本机携带的 `Desktop_QR_Send/example.db`）

| 表/项目 | 只读统计 |
|---|---:|
| `yk_case` | 17,110条 |
| `yk_box_case`（待处理/待上传） | 7,890条 |
| `yk_box_case_history` | 1,642,627条 |
| 本机库中匹配 `/box_` 模拟盒码的待上传记录 | 0条 |
| 数据库文件大小 | 约219MB |

注意：以上是本机数据库，不代表设备电脑2026-07-28测试后数据库的实时内容。设备电脑当时运行模拟逻辑后，按代码路径可能新增了6条 `box_时间戳_序号` 记录。任何清理都必须先复制并备份设备电脑数据库，再按准确ID处理，不能清空整库。

## 三、朋友把4个按钮连通后，需要做什么

朋友先不要只说“能用了”，应把以下资料一起交回来：

1. 修改后的源文件，保持原项目相对路径；优先只提供按钮相关文件。
2. 使用的硬件模块准确型号和接线照片。
3. 实际COM口、波特率、数据位、停止位、校验位、站号。
4. 主机发送查询帧、设备返回帧和CRC规则。
5. 4个按钮分别按下、释放时的原始Hex日志。
6. 按钮通道与物理按钮的对应表。
7. 长按、快速连按时是否会重复上报。
8. 他基于哪个版本修改，是否还改了 `main.py`、`home.py` 或配置文件。

朋友现场首先只需证明通信，不应直接触发打印、写RFID、写数据库或上传服务器。通信层合格日志至少应包含：

```text
串口已打开
收到原始数据 Hex
协议帧CRC校验通过
按钮通道1 按下/释放
按钮通道2 按下/释放
按钮通道3 按下/释放
按钮通道4 按下/释放
```

朋友连通后，后续顺序为：

1. 把朋友版本复制到本机的独立“待合并”目录，不覆盖当前项目。
2. 在本机进行逐文件对比和手工合并。
3. 先做不触发业务的4通道测试。
4. 再接入Qt主线程和页面按钮映射。
5. 修复生产阻断问题后，才测试打印、真实扫码、数据库和服务器。

## 四、朋友代码如何与当前项目合并

### 4.1 合并原则

- 不接受直接覆盖整个 `Desktop_QR_Send` 文件夹。
- 不覆盖 `example.db`、设备电脑的 `config.json`、日志、打印记录和SDK目录。
- 按钮通信应尽量只落在 `utils/RS485Utils.py`；如确实需要，再最小化修改 `main.py`。
- `page/home.py` 保留现有页面和装箱业务，只接收标准化按钮事件，不混入串口收包代码。
- 当前WDIP诊断代码在合并完成后退出生产路径，但只读探针可以保留在开发工具中。

### 4.2 推荐合并步骤

1. **冻结当前版本**  
   在本机先建立日期快照或Git分支，记录当前5个修改文件和未跟踪资源。此动作应在真正收到朋友代码后执行。

2. **隔离朋友代码**  
   放入例如 `incoming_friend_buttons_20260729` 的独立目录，保留朋友原始文件，禁止直接覆盖。

3. **逐文件比较**  
   重点比较：
   - `Desktop_QR_Send/utils/RS485Utils.py`
   - `Desktop_QR_Send/main.py`
   - `Desktop_QR_Send/page/home.py`
   - 串口或协议相关配置文件

4. **抽取通信层**  
   保留朋友已经实机验证的串口参数、查询帧、拆包、CRC和通道识别代码；保留当前项目已有的按下沿触发、300ms消抖、Qt主线程队列投递和页面映射。

5. **统一按钮事件接口**  
   通信层最终只需要向上层报告：

   ```text
   channel: 1~4
   is_press: True/False
   raw_frame: 原始Hex（日志用）
   ```

6. **固定生产映射**

   | 通道 | 物理按钮 | 页面入口 |
   |---:|---|---|
   | 1 | 设备复位 | `button_cancel` / `on_button_cancel_clicked()` |
   | 2 | 打印箱码 | `button_print` / `on_button_print()` |
   | 3 | 拍照识别 | `button_again` / `on_button_again()` |
   | 4 | 重新装箱 | `button_reset` / `reset_data()` |

   如果朋友实测通道顺序不同，以实物日志为准，只调整映射表，不改业务函数。

7. **离线验证**  
   检查语法、CRC、分段包、粘包、错误CRC、按下/释放、长按、消抖、Qt线程投递。

8. **设备电脑回归**  
   覆盖按钮相关文件，完整退出旧进程后启动；先只看通道日志，再逐项开放业务动作。

### 4.3 朋友按钮代码的最低验收标准

- 程序启动后能稳定打开正确COM口，不扫描或占用无关COM口。
- 连续运行30分钟不掉线；拔插转换器后能恢复或明确提示。
- 每个按钮按50次：无漏触发、无双触发、无串键。
- 长按只触发一次；释放不触发业务动作。
- CRC错误、半包、粘包不会误触发页面。
- 串口线程不直接操作Qt控件。
- 生产模式不自动轮询多种写功能码，不写设备寄存器。

## 五、交付前必须修复的问题

### P0：不修不能进入生产测试

1. **替换当前WDIP诊断模式**  
   合并朋友实机成功的顶部按钮协议，并将实际COM和参数写入生产配置。

2. **删除运行时模拟盒码回退**  
   以下生产路径必须移除：
   - `home.py` 中无箱码时自动生成箱码；
   - `home.py` 中无识别结果时自动生成6条 `box_...` 并强制录入；
   - `pyzbar_utils.py` 中识别失败时自动生成6条模拟盒码；
   - 屏幕“确认录入”默认调用 `force=True` 绕过数量校验。

3. **修复打印真假成功**  
   当前流程在打印失败时仍保存新箱码、更新绿框、播放成功音并提示成功。生产流程必须改为：打印机明确成功后才保存和启用新箱码；失败保持原状态并显示可操作错误。

4. **确认是否需要RFID写卡**  
   当前打印入口会默认写EPC。交付前必须由业务确认：
   - 只打印纸质条码；或
   - 打印并写RFID；或
   - 打印、写入并读回核验。

   未确认前不能把按钮2直接用于生产。若需要闭环核验，必须真实比较写入值和读回EPC/TID，不能无条件将结果设为成功。

5. **修复WebSocket事件循环**  
   `startWS()` 当前先把连接任务挂到一个未运行的默认事件循环，随后 `main.py` 又新建另一个循环。应让连接、发送、接收和关闭全部运行在同一个后台循环中。

6. **隔离测试与生产数据**  
   测试阶段使用备份数据库或专用测试库，并保持生产服务器断开；只有经过明确授权的接口联调阶段才能发送测试数据到服务器。

### P1：生产验收前必须完成

1. 恢复盒码重复校验；当前重复检查代码被注释。
2. 分清 `edit_max_jian`（一捆盒数）和 `edit_max_xiang`（一箱捆数）；当前部分代码把同一个值同时用于两层上限。
3. 打印已有箱码时应明确区分“补打原箱码”和“生成新箱码”；补打不能清空装箱进度或生成新码。
4. 打印、识别、确认录入、重新装箱增加统一忙锁，避免并发和快速连按。
5. 重新装箱增加二次确认，明确只清当前上下文，不删除已落库记录。
6. WebSocket发送需要等待实际发送结果；手动上传不能在Future尚未完成时显示100%。
7. echo回执后的“写历史表 + 删除待上传表”应放在同一数据库事务中。
8. 标签上的产品名称、规格、箱规、保质期、生产商等目前来自硬编码默认值；必须确认真实来源，不能用默认示例冒充生产数据。
9. `config.json` 当前打印机为 `HPRT N31`，但代码实际调用T63R SDK；必须统一实际打印机型号和配置。
10. 当前串口配置为 `COM21`，设备运行时自动回退到COM3；生产版应保存并严格使用实测COM口，避免误占其他串口。

### P2：打包交付前完成

1. 更新 `装箱扫码.spec`，加入RFID SDK、DLL、配置、模板、资源和新增页面。
2. 旧 `dist/装箱扫码` 是2026-05构建，不能作为本次发布包。
3. 建立明确依赖清单和安装说明，不再使用当前占位README。
4. 生产包排除探针、测试脚本、测试图片、测试CSV和无关SDK示例资源。
5. 增加版本号、发布日期、变更记录、配置备份和回滚包。
6. 控制日志频率和保留周期，避免长期运行产生超大日志。

## 六、完整测试计划

### 6.1 测试前准备

1. 备份设备电脑：
   - `Desktop_QR_Send`完整源码；
   - `config.json`；
   - `example.db`；
   - `records`和`logs`。
2. 第一阶段断开生产服务器或使用明确的测试环境，防止测试盒码上传。
3. 准备可识别的测试盒码、测试标签纸和允许测试的RFID标签。
4. 明确一捆多少盒、一箱多少捆、标签尺寸、打印份数和RFID要求。
5. 所有会写数据、打印或写RFID的测试，在执行前单独说明并记录测试编号。

### 6.2 顶部4按钮测试

| 编号 | 测试内容 | 预期结果 |
|---|---|---|
| B01 | 按钮1单击 | 只执行一次设备复位，相机恢复实时画面 |
| B02 | 按钮2单击 | 只执行一次打印流程；失败不生成或启用新箱码 |
| B03 | 按钮3单击 | 只触发一次真实拍照识别，无码时明确失败，不产生模拟数据 |
| B04 | 按钮4单击 | 弹出重新装箱确认；取消不改变数据，确认后只清当前上下文 |
| B05 | 每个按钮连续按50次 | 无漏触发、无双触发、无串键 |
| B06 | 长按3秒 | 只触发一次业务动作 |
| B07 | 两个按钮快速交替 | 忙锁生效，不产生并发打印/识别 |
| B08 | 拔插RS485转换器 | 明确报错并能够恢复连接或提示重启 |

### 6.3 打印机与RFID测试

| 编号 | 测试内容 | 预期结果 |
|---|---|---|
| P01 | 打印机正常单张打印 | 纸张尺寸、方向、位置、浓度正确，条码可被扫码枪识别 |
| P02 | 连续打印20张 | 无丢张、重复、卡死、错码，箱码与标签一一对应 |
| P03 | 打印机断电/USB拔出 | 页面明确失败，不显示成功、不切换箱码 |
| P04 | 缺纸、开盖、卡纸 | 给出对应错误；恢复后可重试原箱码 |
| P05 | 补打当前箱码 | 使用原箱码，不清空当前装箱进度，不新增箱码记录 |
| P06 | 快速重复按打印按钮 | 忙锁只允许一个打印任务 |
| P07 | 打印份数与配置 | 实际份数和配置一致，不能忽略 `page_num/copies` |
| P08 | 如果启用RFID写入 | 写入EPC与箱码规则一致，读回后真实比较；失败不能报PASS |
| P09 | 相同RFID/箱码重复写入 | 按生产规则拦截或明确允许补打但禁止错误覆写 |

### 6.4 装箱扫码页面功能与BUG测试

| 编号 | 测试内容 | 预期结果 |
|---|---|---|
| S01 | 未打印箱码直接识别 | 提示先打印箱码，不自动生成箱码 |
| S02 | 镜头中没有二维码 | 明确提示未识别，不生成6条模拟盒码 |
| S03 | 单次识别正确数量 | 下侧盒码格、数量和实际识别结果一致 |
| S04 | 数量不足或超出一捆上限 | 不录入，提示准确原因 |
| S05 | 同一画面重复二维码 | 单次去重正确 |
| S06 | 已录入盒码再次扫码 | 跨批次重复校验生效，不重复落库 |
| S07 | 连续完成一捆 | 装箱进度只增加1，不发生重复录入 |
| S08 | 连续完成一箱 | 达到配置捆数后禁止继续，提示后续动作 |
| S09 | 第11捆/越界操作 | 页面不出现11/10等越界状态 |
| S10 | 设备复位 | 只清未录入识别结果，不破坏当前已录入装箱进度 |
| S11 | 重新装箱取消/确认 | 取消无变化；确认只清当前上下文，不删除数据库历史 |
| S12 | 程序中途重启 | 当前箱码和装箱进度按业务要求正确恢复 |
| S13 | 中文、异常字符、非目标域二维码 | 程序不崩溃，按业务规则拒绝或处理 |
| S14 | 相机断开后重连 | 页面可恢复预览和识别，错误提示清楚 |
| S15 | 连续完成至少5箱 | 无内存持续增长、界面卡死、重复数据或状态错乱 |

### 6.5 数据库与服务器测试

此阶段会写数据库；连接生产服务器前必须再次确认。

| 编号 | 测试内容 | 预期结果 |
|---|---|---|
| D01 | 断网录入 | 只写本地待上传表，未上传计数正确 |
| D02 | 恢复网络 | 只上传未上传记录，不重复发送 |
| D03 | 收到echo回执 | 同一事务迁移到历史表并删除待上传记录 |
| D04 | 服务器返回error | 本地记录保留，页面和日志显示失败原因 |
| D05 | 上传中断电/杀进程 | 重启后数据不丢、不重复核销 |
| D06 | 手动上传大量记录 | 进度基于真实完成/回执，不是假100% |
| D07 | 测试数据隔离 | 测试盒码不会混入生产远程数据；测试结束可精确追踪和清理 |

### 6.6 稳定性与异常恢复

- 连续运行8小时，监控内存、CPU、日志增长和界面响应。
- 相机、串口、打印机分别断开重连。
- 网络断开30分钟后恢复。
- 快速交替操作4个实体按钮和屏幕按钮。
- Windows重启后从 `start.bat` 或正式发布包恢复。
- 程序关闭时正确停止相机、串口、WebSocket和打印SDK，不保留僵尸进程或占用端口。

## 七、测试完成后的生产化清理

生产化不是简单删除所有测试文件，而是保证**正式运行路径绝不自动制造假数据**，同时把必要的诊断工具从生产入口隔离。

### 必须从生产运行路径删除或关闭

- 无二维码时自动生成6条 `box_...` 数据。
- 无箱码时自动生成时间戳箱码并继续识别。
- `force=True` 强制绕过一捆数量限制。
- 打印默认 `allow_reprint=True`。
- 硬编码产品名称、规格、箱规等未确认标签内容。
- RFID未读回却直接标记PASS。
- 假条纹条码预览被描述为真实打印效果。
- 自动扫描多协议、多站号的诊断行为进入正式主程序。

### 可以保留在开发源码、但不能混入生产操作入口

- `rs485_input_probe.py`
- `start_rs485_input_probe.bat`
- `diagnose_print.py`
- `test_rfid_printer.py`
- 离线协议单元测试和测试图片

这些工具应放入单独的 `tools` 或 `tests` 目录，并在打包配置中排除。

### 测试数据库清理规则

1. 先复制备份数据库。
2. 只按测试批次、明确前缀、时间范围和准确ID列出待清理记录。
3. 人工复核清单后再删除。
4. 不执行“清空整表”，不动远程生产数据，不删除无法确认来源的历史记录。
5. 清理后复核待上传计数、历史计数和当前页面缓存。

## 八、最终交付物

项目达到交付时应提供：

1. 已合并并打版本号的生产源码。
2. 可回滚的上一版本备份。
3. 在干净Windows环境验证过的正式发布包。
4. 设备电脑生产配置清单：COM口、串口参数、相机、打印机、服务器地址、数量上限、标签配置。
5. 数据库备份、升级和恢复说明。
6. 顶部按钮协议及4通道映射文档。
7. 打印机/RFID配置和故障处理说明。
8. 完整测试记录：每项结果、日志、照片、打印样张和发现的BUG。
9. 未解决问题清单和业务限制。
10. 部署、启动、关闭、升级和回滚操作说明。

## 九、最终交付验收标准

满足以下条件后才能标记为“生产可交付”：

- 4个实体按钮各50次测试无漏触发、双触发和串键。
- 实体按钮与屏幕按钮执行同一业务逻辑，且线程安全。
- 真实二维码识别失败时不产生任何模拟数据。
- 装箱数量、重复校验、满捆和满箱边界正确。
- 打印失败不生成/启用新箱码；补打不换码、不清进度。
- 若启用RFID，写入和读回比对有真实证据；若不启用，生产代码不写RFID。
- 断网缓存、恢复补传和echo核销通过，并且没有重复远程数据。
- 设备电脑连续运行8小时无崩溃、卡死和不可控资源增长。
- 生产包中没有自动模拟盒码、测试服务器、测试数据库写入入口和虚假PASS。
- 发布包在干净设备环境安装启动、硬件连接和完整5箱流程验收通过。

## 十、推荐的后续执行顺序

1. 等朋友完成按钮通信并取得源文件、参数和原始Hex日志。
2. 在本机冻结当前版本并手工合并按钮代码。
3. 在设备电脑先完成纯按钮通道测试。
4. 修复本文件列出的P0生产阻断问题。
5. 使用隔离测试库完成页面、打印机和装箱流程测试。
6. 修复测试发现的BUG并重复回归。
7. 在明确授权后完成WebSocket真实接口联调。
8. 删除生产运行路径中的模拟逻辑，精确清理测试数据。
9. 更新打包配置，制作候选生产版本。
10. 完成连续运行和完整5箱现场验收，保留日志、照片和打印样张后交付。

---

本次状态审计只读取了代码、配置、日志和本机数据库，没有运行正式业务、没有写数据库、没有打印、没有写RFID，也没有连接或修改远程服务器数据。
