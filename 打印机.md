# 汉印 T63R RFID 打印写卡闭环系统技术与集成指南

> 更新时间：2026年7月27日  
> 适用工程：`Desktop_QR_Send` (激光扫码主工程)  
> 支持设备：汉印 JIEPRT T63R / T63RZ 工业级超高频 RFID 标签打印机

---

## 一、 模块架构概述

本模块为“激光扫码”工程提供了**工业级 T63R RFID 标签打印、EPC 13位箱码写卡、TID 实时读回核验、二次防重复重写拦截及多标签连续流水线打印**的完整闭环支持。

工程目录结构集成如下：
```text
Desktop_QR_Send/
├── vendor/t63r_x64/          # 64 位底层 DLL 动态库及关联依赖 (libDSThermal.dll)
├── rfid_printer/             # RFID 核心引擎逻辑包
│   ├── sdk.py                # 64 位 WinDLL ctypes 低级接口封装 (防多目录加载丢失)
│   ├── workflow.py           # RfidPrintService 核心业务工作流 (包含单张锁、批量流水线)
│   ├── encoder.py            # 13位箱码格式校验、保留前导零、Hex 编解码器
│   ├── models.py             # LabelPrintData 生产模板与 PrintResult 比对结果模型
│   └── errors.py             # 统一异常定义
├── config/settings.json      # 100x80mm 300DPI “高原安”生产箱标签模版配置文件
├── utils/printUtils.py       # 统一对外暴露的打印工具库接口
└── test_rfid_printer.py      # 现场设备独立测试与比对验证工具脚本
```

---

## 二、 核心功能与技术规范

| 序号 | 技术功能 | 实现方式与参数规范 | 业务价值 |
| :---: | :--- | :--- | :--- |
| **1** | **64位 DLL 环境适配** | ctypes `WinDLL` 标准 `__stdcall` 绑定，结合 `runtime_bootstrap.py` 动态加载。 | 零冲突运行在设备 PC 的 64 位 Python 与 PySide6 环境中。 |
| **2** | **13位箱码规范** | 强制要求刚好 13 位纯数字，以字符串格式全程传输，**严格保留前导零** (如 `0001234567890`)。 | 确保系统内箱码格式 100% 统一。 |
| **3** | **RFID 13位 EPC 写入** | 采用 **Hex 直读模式** (`data_format_code = 2`)，将 13 位箱码作为 Hex 前缀写入芯片 EPC 区，如 `012345678902000000000000`。 | 盘点网关或扫描读卡器读取 JSON 里的 `"epc"` 字段时，**前 13 位直接就是箱码**。 |
| **4** | **300 DPI 高清排版** | 自动匹配 T63RZ 300 DPI 印头，以 UTF-8 字体画图渲染“高原安” 9 大生产字段。 | 解决文字模糊与一维条码重影问题。 |
| **5** | **TID 闭环实时比对** | 打印走纸的同一次任务中，同步读回芯片 TID 卡号与 EPC 数据，比对一致输出 `[PASS]`。 | 做到“印一张、写一张、验一张”，防漏写错写。 |
| **6** | **二次防重复拦截** | 数据库/CSV 记录自动查重，若标签已被打印写卡过，再次请求时 **3.8毫秒内软件拦截**。 | 打印机不走纸、不吐标签，**芯片内 RFID 数据绝不会被二次覆盖或篡改**。 |
| **7** | **连续流水线打印** | 提供 `batch_print_write_verify` 批量接口，自动流式控制连打 N 张。 | 适应工业批量装箱场景。 |

---

## 三、 接口使用与代码集成指南

### 1. 从“装箱扫码”页面或按键中调用

在 `Desktop_QR_Send/page/home.py` 或页面点击/硬件按键事件函数中直接调用：

```python
from rfid_printer import RfidPrintService, LabelPrintData
from utils.printUtils import print_rfid_box_label

# 示例：当激光扫码获取到 13 位箱码，且 API 返回了产品属性时
scanned_box_code = "0123456789020"  # 激光扫码设备获取

# 组合标签生产属性
label_info = LabelPrintData(
    box_code=scanned_box_code,
    brand="高原安",
    product_name="高原安藏式甜茶",            # 后续来自于 HTTP 接口
    spec="200g(20g*10条)/盒",                # 后续来自于 HTTP 接口
    box_spec="200g*40盒/箱",                 # 后续来自于 HTTP 接口
    shelf_life="18个月",
    produce_date="2026/07/27",
    storage="干燥、阴凉、通风处",
    manufacturer="乌兰察布蒙帝乳业有限责任公司"
)

# 触发单张打印、EPC 写入与闭环比对
result = print_rfid_box_label(scanned_box_code, label_data=label_info)

if result.success:
    print(f"✅ 装箱打印写卡成功！TID: {result.read_tid}, 耗时: {result.elapsed_ms}ms")
else:
    print(f"❌ 打印未执行/拦截: {result.error_message}")
```

### 2. 连续批量打印接口调用

若从接口获取到了一批 N 盒/箱的数据列表，可一次性连续触发：

```python
from rfid_printer import RfidPrintService, LabelPrintData

service = RfidPrintService()

# 批量列表
items = [
    LabelPrintData(box_code="0123456789021", product_name="高原安藏药红景天胶囊"),
    LabelPrintData(box_code="0123456789022", product_name="高原安藏红花速溶茶"),
    LabelPrintData(box_code="0123456789023", product_name="高原安青稞黑枸杞冲剂"),
]

# 批量连打写卡
results = service.batch_print_write_verify(items)
```

---

## 四、 现场设备测试指令说明

在现场设备 PC 上，打开命令行控制台并进入 `Desktop_QR_Send` 目录：
```powershell
cd "D:\高原安项目\激光扫码2\激光扫码\Desktop_QR_Send"
```

可以使用 `test_rfid_printer.py` 进行 4 大项自动化测试：

### 1. 测试 1：探测打印机连接
```powershell
python test_rfid_printer.py --probe
```
* **作用**：检查 USB 打印机是否通电、识别 SN 序列号及固件版本。

### 2. 测试 2：触发纸张缝隙与 RFID 天线自动定位校准
```powershell
python test_rfid_printer.py --calibrate
```
* **作用**：当更换标签纸卷后，按此命令让打印机自动对齐缝隙与天线。

### 3. 测试 3：执行单张打印与写卡核验
```powershell
python test_rfid_printer.py --code 0123456789020
```
* **作用**：吐出 1 张标签，印出完整中文字样，写入 EPC `012345678902000000000000` 并读回比对。

### 4. 测试 4：二次防重复打印拦截验证
```powershell
# 再次运行相同命令：
python test_rfid_printer.py --code 0123456789020
```
* **预期**：在 3.8 毫秒内提示 `[FAIL] 防二次打印拦截！`，打印机不吐纸，芯片不覆盖。

### 5. 测试 5：连续 3 张流水线连打测试
```powershell
python test_rfid_printer.py --batch3
```
* **作用**：连续吐出 3 张不同箱码标签，依次完成写卡与核验。

---

## 五、 现场常见问题排查 (FAQ)

1. **若提示 `[FAIL] 未检测到通电连接的 USB T63R 打印机`**：
   - 检查打印机后方电源开关是否处于开启状态（绿灯亮）；
   - 检查打印机与设备 PC 之间的 USB 数据线是否插紧。

2. **若打印机吐纸不止或打在一半**：
   - 执行 `python test_rfid_printer.py --calibrate` 或按打印机面板的【0 进纸】键对齐缝隙。

3. **若盘点网关读取不到 EPC 13 位箱码**：
   - 确认 [config/settings.json](file:///d:/高原安项目/激光扫码2/激光扫码/Desktop_QR_Send/config/settings.json) 中 `data_format_code` 为 `2` (Hex 模式)。网关取前 13 位字符串即为箱码。
