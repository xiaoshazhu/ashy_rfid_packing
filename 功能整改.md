# 激光扫码装箱识别系统 - 四按钮功能整改参考

> **文档版本**：V1.0.0  
> **编制日期**：2026-07-10  
> **整改目标**：将现场主要实体按钮统一为“打印箱码、重新装箱、拍照识别、复位”四项功能  
> **说明**：本文仅提供整改设计和代码位置参考，未修改任何现有源码、配置、数据库或发布程序。

---

## 一、整改结论先行

现有代码已经具备这四项功能所需的大部分基础方法，后续开发不需要重写相机、打印或数据库模块，主要工作是：

1. 重新确认四个实体按钮分别对应哪个 RS485 通道；
2. 统一按钮触发沿，避免同一按钮按下和释放执行两个不同动作；
3. 在 `RS485Utils.process_data` 中重新映射四个通道；
4. 将主界面已有的“重置”明确改名为“重新装箱”；
5. 为“打印箱码”和“重新装箱”增加确认及作业状态保护；
6. 明确“重新装箱”是否只清除当前作业状态，还是需要撤销已经落库/上传的绑定记录。

推荐的四按钮逻辑映射如下。该编号与产品说明书中按钮排列“复位、打印箱码、拍照识别、重新装箱”保持一致：

| RS485 通道 | 推荐按钮 | 推荐触发时机 | 建议调用的业务入口 |
| :---: | :--- | :---: | :--- |
| 1 | 复位 | 按下 | `Home.on_button_cancel_clicked()` |
| 2 | 打印箱码 | 按下 | `Home.on_button_print()` |
| 3 | 拍照识别 | 按下 | `Home.on_button_again()` |
| 4 | 重新装箱 | 按下后确认 | 新增业务包装方法，内部按规则调用 `Home.reset_data()` |

> [!IMPORTANT]
> 上表是推荐的**逻辑编号**，不能在未核对接线的情况下直接认定现场通道号。现有代码中的通道 1 同时被注释为光感触发，因此整改前必须通过日志逐个按实体按钮，确认实际通道号、按下状态和释放状态。若现场接线顺序不同，只调整映射表，不必改变四项业务定义。

---

## 二、当前代码中的按钮到底是什么作用

### 2.1 当前主界面按钮

当前 UI 中与整改相关的按钮如下：

| Qt 控件名 | 当前界面文字 | 当前连接函数 | 当前实际作用 |
| :--- | :--- | :--- | :--- |
| `button_print` | 打印箱码 | `Home.on_button_print` | 生成毫秒时间戳箱码、写入 `yk_case`、打印 CODE128 标签并设为当前箱码 |
| `button_ok` | 确认录入 | `Home.force_ok_clicked` | 以 `force=True` 录入当前识别结果，会绕过“一捆数量”校验 |
| `button_cancel` | 设备复位 | `Home.on_button_cancel_clicked` | 停止相机、关闭设备、重新初始化相机、清除本次识别结果并恢复实时画面 |
| `button_again` | 手动识别 | `Home.on_button_again` | 检查当前箱码，读取相机当前帧并进入二维码识别流程 |
| `button_reset` | 重置 | `Home.reset_data` | 清除当前箱码、当前识别结果和当前箱内捆进度，但不删除数据库记录 |

从现有方法语义来看：

* `button_print` 可继续作为“打印箱码”；
* `button_reset` 最接近目标“重新装箱”；
* `button_again` 可继续作为“拍照识别”；
* `button_cancel` 可继续作为“复位”；
* `button_ok` 不在新的四按钮目标中，可保留为屏幕上的管理/强制录入功能，但不再占用实体按钮通道。

### 2.2 当前 RS485 四通道映射

文件位置：`Desktop_QR_Send/utils/RS485Utils.py`，函数：`RS485Utils.process_data`。

当前协议将三字节数据解析为：

```text
AA NN SS
│  │  └─ 状态：01 表示按下，其他值按释放处理
│  └──── 通道编号
└─────── 固定帧头
```

当前实际映射为：

| 通道 | 收到状态 | 当前动作 | 当前调用方式 |
| :---: | :---: | :--- | :--- |
| 1 | 按下 `01` | 延时 0.5 秒后拍照识别 | 模拟点击 `button_again` |
| 1 | 释放 | 设备复位 | 模拟点击 `button_cancel` |
| 2 | 释放 | 确认录入/强制录入 | 模拟点击 `button_ok` |
| 3 | 按下 `01` | 再次拍照识别 | 模拟点击 `button_again` |
| 4 | 释放 | 打印箱码 | 模拟点击 `button_print` |

当前映射存在三个需要整改的问题：

1. **通道 1 一次按压过程执行两个动作**：按下时拍照，释放时复位，容易刚识别完就清除画面。
2. **触发沿不统一**：通道 1/3 在按下执行，通道 2/4 在释放执行，维护人员不容易理解。
3. **通道 2 是强制录入**：当前 `button_ok` 会绕过数量校验，不符合目标四按钮，也存在误录风险。

---

## 三、目标四按钮的功能定义

整改前应先固定业务含义，避免后续开发人员只改按钮名称却没有改数据行为。

### 3.1 打印箱码

#### 目标功能

* 创建一个新的唯一箱码；
* 将箱码写入本地箱码表；
* 打印 CODE128 标签；
* 将新箱码设置为当前装箱箱码；
* 后续识别的盒码绑定到该箱码。

#### 可复用的现有方法

```text
Home.on_button_print()
├─ Home.generate_case_code()
├─ Database.case_insert_data()
├─ Home.print_barcode()
└─ Home.updataPageCase()
```

#### 当前风险

当前已有箱码时再次打印，`on_button_print` 会清空本次识别结果和当前捆进度，然后直接生成新箱码。若旧箱尚未完成，会造成界面作业上下文丢失。

#### 推荐整改

打印前根据状态分三种情况：

| 当前状态 | 推荐行为 |
| :--- | :--- |
| 无当前箱码 | 直接生成并打印 |
| 有箱码但当前箱内没有已录入捆 | 提示“是否重新打印/更换箱码”，确认后执行 |
| 有箱码且已有装箱进度 | 阻止直接打印，提示先完成当前箱或执行“重新装箱” |

建议区分“打印新箱码”和“补打当前箱码”：

* **打印新箱码**：生成新编号并切换当前箱；
* **补打当前箱码**：不生成新编号，只重新打印 `self.case`。

这样可以防止标签损坏时为了补打而意外换箱。

### 3.2 重新装箱

“重新装箱”是本次整改中最需要明确的数据功能，不能只看界面名称。

#### 推荐的安全定义

重新装箱用于放弃当前尚未完成的装箱上下文，使程序回到“请打印箱码”状态：

* 清除本次尚未录入的识别结果；
* 清除当前箱码；
* 清除当前界面的捆进度；
* 清除 `config.json` 中的 `caseCode` 和 `caseData`；
* 不删除累计统计；
* 不直接删除 SQLite 中已经落库的数据；
* 不直接撤销已经上传到服务端的记录。

现有 `Home.reset_data()` 基本符合上述“清当前状态、不删历史记录”的安全定义，可作为整改基础。

#### 必须增加的确认

推荐按钮按下后显示：

```text
确认重新装箱？

当前箱码：1776150077913
已完成：3 捆

重新装箱将清除当前箱码和页面装箱进度，
但不会自动撤销已经保存或上传的盒码绑定记录。

[取消] [确认重新装箱]
```

#### 如果业务要求撤销已绑定记录

现有代码不能安全实现“完整撤销后重新装箱”，原因是：

1. 已录入数据可能仍在 `yk_box_case` 待上传表；
2. 也可能已经收到回执并进入 `yk_box_case_history`；
3. 还可能已经写入云端系统；
4. 当前 WebSocket 协议只有新增消息和回执，没有“解绑、作废、冲销”接口。

如果现场要求把已录入盒码从旧箱解绑，后续必须先增加服务端接口或消息类型，例如：

```json
{
  "type": "cancel_case",
  "data": {
    "caseContent": "1776150077913",
    "reason": "重新装箱",
    "operator": "班组长",
    "requestId": "全局唯一请求ID"
  }
}
```

服务端确认冲销成功后，客户端才能把旧箱标记为已作废。生产数据不建议直接 `DELETE`，应保存作废原因、操作人、时间和原绑定关系。

### 3.3 拍照识别

#### 目标功能

* 要求已有当前箱码；
* 从工业相机连续流中获取当前帧；
* 识别画面内二维码并去重；
* 显示识别数量和标框图像；
* 数量达到“一捆数量”时按现有流程自动录入；
* 数量不足时提示失败，不通过实体按钮强制录入。

#### 可复用的现有方法

```text
Home.on_button_again()
└─ Home.capture_and_save_image()
   └─ utils.pyzbar_utils.process_image()
      ├─ 图像预处理和二维码解码
      ├─ Home.on_button_ok_clicked(force=False)
      └─ Home.scan_code()
```

#### 重要说明

当前 `process_image` 在识别完成后会自动调用普通录入校验，因此目标四按钮中可以不设置独立“确认录入”实体按钮。标准满捆自动录入；尾箱或不足数量的强制录入仍建议保留在屏幕管理功能中，并增加权限或二次确认。

#### 推荐防重复措施

* 拍照识别执行期间锁定该按钮；
* 识别完成或超时后再解锁；
* 300～800 ms 内的重复硬件事件只处理一次；
* 已显示定格结果时再次拍照，应先恢复实时画面再取新帧；
* 同一盒码是否允许跨捆/跨箱重复，需要由业务规则和数据库共同校验。

### 3.4 复位

#### 目标功能

复位用于恢复相机和采集状态，不等于重新装箱：

* 停止相机取流；
* 关闭相机设备；
* 重新枚举、打开相机并开始取流；
* 清除本次尚未录入的识别结果；
* 恢复实时画面；
* 保留当前箱码；
* 保留已经完成的捆进度；
* 不删除数据库记录。

#### 可复用的现有方法

```text
Home.on_button_cancel_clicked()
├─ Home.stop_grabbing()
├─ Home.close_device()
├─ Home.hiKInit()
├─ Home.scan_code_end()
└─ Home.stop_line()
```

#### 与重新装箱的区别

| 对比项 | 复位 | 重新装箱 |
| :--- | :---: | :---: |
| 重启相机 | 是 | 通常不需要 |
| 清除本次未录入识别结果 | 是 | 是 |
| 保留当前箱码 | 是 | 否 |
| 保留当前箱捆进度 | 是 | 否 |
| 删除数据库历史 | 否 | 默认也不删除 |
| 典型场景 | 相机无画面、识别画面卡住 | 物料放错、放弃当前箱并重新开始 |

---

## 四、整改前必须完成的通道确认

不要仅根据机柜表面标签或代码中的 `door_number` 猜测通道。建议在不放置生产物料的情况下完成以下确认。

### 4.1 通道识别步骤

1. 启动程序并打开日志窗口，或查看 `logs/app_log.log`。
2. 依次只按一个实体按钮一次。
3. 记录日志中的“开关 N 被按下”和“开关 N 被释放”。
4. 每个按钮重复三次，确认通道号稳定。
5. 单独触发光电传感器，确认它是否也占用通道 1。
6. 记录按钮是否同时发送按下与释放，还是只发送一个状态。
7. 形成现场接线表并拍照留档。

### 4.2 建议填写的现场确认表

| 机柜标签 | 实际通道号 | 按下帧 | 释放帧 | 是否与传感器共用 | 整改后功能 |
| :--- | :---: | :--- | :--- | :---: | :--- |
| 按钮 A | 待确认 | 待确认 | 待确认 | 待确认 | 复位 |
| 按钮 B | 待确认 | 待确认 | 待确认 | 待确认 | 打印箱码 |
| 按钮 C | 待确认 | 待确认 | 待确认 | 待确认 | 拍照识别 |
| 按钮 D | 待确认 | 待确认 | 待确认 | 待确认 | 重新装箱 |
| 光电传感器 | 待确认 | 待确认 | 待确认 | - | 自动拍照或仅物料到位信号 |

> [!CAUTION]
> 如果光电传感器确实与四个实体按钮之外单独存在，它不应为了迁就“四按钮”而复用同一逻辑通道。建议将传感器事件和按钮事件分开建模，例如 `sensor_arrived` 与 `button_pressed`，否则传感器离开可能误触发复位。

---

## 五、建议修改的代码位置（仅供后续开发参考）

### 5.1 `utils/RS485Utils.py`

整改重点：只负责解析硬件事件并发送明确的业务信号，不再让同一通道按下/释放执行不同业务。

推荐逻辑伪代码：

```python
# 这是整改参考伪代码，不是对现有文件的修改。

BUTTON_RESET = 1
BUTTON_PRINT = 2
BUTTON_CAPTURE = 3
BUTTON_REPACK = 4


def process_switch_event(channel, is_pressed):
    # 实际硬件如果只在释放时稳定上报，可统一改为处理释放；
    # 核心要求是四个按钮使用同一种规则。
    if not is_pressed:
        return

    if is_debounced(channel):
        return

    if channel == BUTTON_RESET:
        emit_reset_signal()
    elif channel == BUTTON_PRINT:
        emit_print_signal()
    elif channel == BUTTON_CAPTURE:
        emit_capture_signal()
    elif channel == BUTTON_REPACK:
        emit_repack_signal()
```

不建议继续在线程中调用 `button_xxx.click()`。RS485 监听运行在后台线程，Qt 控件应由主线程操作。推荐定义 Qt Signal：

```text
RS485线程解析数据
      │
      ├─ reset_requested
      ├─ print_requested
      ├─ capture_requested
      └─ repack_requested
      │
      ▼
Qt主线程连接到 Home 对应业务方法
```

### 5.2 `page/home.py`

建议保留现有四个核心方法，并增加两个业务包装入口：

| 建议入口 | 内部复用 | 目的 |
| :--- | :--- | :--- |
| `request_print_case()` | `on_button_print()` | 打印前检查当前箱状态，区分新箱码和补打 |
| `request_repack()` | `reset_data()` | 弹出确认，记录旧箱码和进度，确认后清理当前作业状态 |

建议的状态检查伪代码：

```python
# 这是整改参考伪代码，不是对现有文件的修改。

def request_print_case(self):
    if self.case is None:
        self.on_button_print()
        return

    if self.scan_case_data:
        show_message("当前箱已有装箱数据，请先完成或重新装箱")
        return

    if confirm("是否生成新的箱码？"):
        self.on_button_print()


def request_repack(self):
    old_case = self.case
    bundle_count = len(self.scan_case_data)

    if not old_case and not bundle_count and not self.sacn_box_data:
        show_message("当前没有需要重新装箱的数据")
        return

    if confirm_repack(old_case, bundle_count):
        write_operation_log("重新装箱", old_case, bundle_count)
        self.reset_data()
```

如果未来增加服务端冲销，`request_repack` 应先发送冲销请求并等待成功回执，不能先清本地数据再请求服务端。

### 5.3 `qtDesigner/qt_home.ui`

建议在 Qt Designer 中调整，而不是直接手改生成的 `qt_home.py`：

| 当前控件 | 建议显示文字 | 业务连接 |
| :--- | :--- | :--- |
| `button_print` | 打印箱码 | `request_print_case` |
| `button_reset` | 重新装箱 | `request_repack` |
| `button_again` | 拍照识别 | `on_button_again` |
| `button_cancel` | 复位 | `on_button_cancel_clicked` |
| `button_ok` | 强制录入（管理） | 保留屏幕入口，建议增加权限和二次确认 |

重新生成 UI Python 文件后应检查 `Home.eventInit()` 中的信号连接，避免控件名变化造成启动错误。

### 5.4 `page/config.py` 和 `config.json`

建议把按钮通道映射从硬编码迁移到配置：

```json
{
  "button_mapping": {
    "reset": 1,
    "print_case": 2,
    "capture": 3,
    "repack": 4
  },
  "button_trigger_edge": "press",
  "button_debounce_ms": 500
}
```

这样更换远程 IO 模块或现场接线后，只改配置即可，不必重新编译程序。

---

## 六、推荐的整改后业务流程

### 6.1 正常装箱

```text
[打印箱码]
     │
     ▼
[系统生成并打印新箱码]
     │
     ▼
[放置一捆物料]
     │
     ▼
[拍照识别]
     │
     ├─数量符合──> 自动绑定、落库、上传、捆进度 +1
     │
     └─数量不足──> 告警，不自动强制录入
     │
     ▼
[继续下一捆，直至达到一箱数量]
```

### 6.2 相机异常

```text
[相机无画面/定格异常]
          │
          ▼
        [复位]
          │
          ├─重启相机和实时画面
          ├─清本次未录入结果
          └─保留当前箱码及已完成捆进度
```

### 6.3 放错物料或需要放弃当前箱

```text
[按重新装箱]
       │
       ▼
[显示当前箱码和已完成捆数]
       │
       ├─取消──> 保持原状态
       │
       └─确认──> 清当前箱码、临时识别和页面进度
                       │
                       ▼
                 [重新打印箱码]
```

如果旧箱已有落库/上传数据，系统应明确提示“历史绑定未自动撤销”。完整撤销必须等待后续冲销接口实现。

---

## 七、实体按钮防误触设计

### 7.1 统一触发沿

优先采用“按下执行、释放忽略”。如果现场模块在释放沿更稳定，可统一采用“释放执行、按下忽略”，但四个按钮必须保持一致。

### 7.2 软件消抖

每个通道保存最近一次有效触发时间。推荐 500 ms 内的相同通道事件忽略，避免机械抖动造成重复打印或重复拍照。

### 7.3 忙状态锁

| 功能 | 忙状态期间的处理 |
| :--- | :--- |
| 打印箱码 | 打印未完成时忽略再次打印 |
| 拍照识别 | 图像处理和录入校验未完成时忽略再次拍照 |
| 复位 | 已在相机初始化时忽略重复复位 |
| 重新装箱 | 确认弹窗存在或冲销处理中忽略重复请求 |

### 7.4 高风险按钮确认

* 打印新箱码：当前箱已有进度时必须确认或阻止；
* 重新装箱：必须二次确认；
* 强制录入：保留在屏幕管理区，建议要求班组长权限；
* 复位和拍照识别：通常可直接执行。

### 7.5 状态反馈

实体按钮触发后应在界面或声音上给出反馈：

* 打印：显示箱码并提示打印成功/失败；
* 拍照：显示“识别中”，成功后播放成功音；
* 复位：显示“设备复位中/复位完成”；
* 重新装箱：确认后显示“请重新打印箱码”。

---

## 八、整改后验收测试

### 8.1 通道与触发测试

| 编号 | 测试步骤 | 预期结果 |
| :---: | :--- | :--- |
| 1 | 分别按四个按钮一次 | 每个按钮只触发一个目标功能 |
| 2 | 长按每个按钮 | 不重复执行或只执行一次 |
| 3 | 快速连按两次 | 消抖时间内不重复打印、不重复拍照 |
| 4 | 观察按下和释放日志 | 只在配置的触发沿执行业务 |
| 5 | 单独触发光电传感器 | 不误触发复位、打印或重新装箱 |

### 8.2 打印箱码测试

1. 无箱码时打印，生成一个新箱码并打印标签；
2. 当前箱无进度时再次打印，出现确认；
3. 当前箱已有捆时按打印，系统阻止或要求明确确认；
4. 打印机离线时不得把“已打印”当作成功；
5. 补打当前箱码时箱码值保持不变。

### 8.3 拍照识别测试

1. 无箱码时按拍照，提示先打印箱码；
2. 满足一捆数量时自动录入一次；
3. 数量不足时不强制录入；
4. 同一按钮按下和释放不造成“识别后立即复位”；
5. 连续触发时不会产生并发图像处理或重复入库。

### 8.4 复位测试

1. 已有箱码和捆进度时执行复位；
2. 相机重新取流；
3. 当前箱码和捆进度保持不变；
4. 本次未录入识别结果被清除；
5. 数据库待上传记录数量不因复位减少。

### 8.5 重新装箱测试

1. 空状态按重新装箱，提示无需操作；
2. 有箱码无捆时按下，确认后清除箱码；
3. 有箱码有捆时显示箱码和捆数；
4. 点击取消后状态完全不变；
5. 点击确认后页面回到“请打印箱码”；
6. 已落库和已上传记录不被静默删除；
7. 操作日志记录旧箱码、捆数、时间和结果。

### 8.6 回归测试

整改完成后还应回归：WebSocket 实时上传、断线补传、服务端 `echo` 回执、SQLite 历史迁移、标签尺寸、日志导出、程序重启状态恢复及设置保存。

---

## 九、建议实施顺序

1. **只做现场通道确认，不修改业务**：记录四按钮和光电传感器的真实帧。
2. **确定最终通道映射**：由现场、电气和软件负责人共同签字确认。
3. **统一触发沿并增加消抖**：先解决按下/释放重复动作。
4. **映射拍照识别与复位**：这两项数据风险较低，先测试。
5. **整改打印箱码**：增加当前箱状态保护和补打功能。
6. **整改重新装箱**：先采用“只清当前状态、不删除历史”的安全版本。
7. **移除实体强制录入映射**：强制录入只保留为受控屏幕操作。
8. **增加配置化按钮映射**：避免以后换线必须改代码。
9. **完成硬件、数据和网络回归测试**。
10. **如需完整撤销绑定，再单独设计服务端冲销协议**。

---

## 十、最终建议映射确认单

后续开发实施前，请以现场实测结果补全并确认下表：

| 功能 | 默认推荐通道 | 现场实测通道 | 触发沿 | 对应现有/建议方法 | 是否确认 |
| :--- | :---: | :---: | :---: | :--- | :---: |
| 复位 | 1 |  | 按下 | `on_button_cancel_clicked` |  |
| 打印箱码 | 2 |  | 按下 | `request_print_case` → `on_button_print` |  |
| 拍照识别 | 3 |  | 按下 | `on_button_again` |  |
| 重新装箱 | 4 |  | 按下 | `request_repack` → `reset_data` |  |
| 光电传感器 | 独立确认 |  | 按现场协议 | 建议独立事件 `sensor_arrived` |  |

签字确认建议包含：产线负责人、电气负责人、软件负责人和验收人员。确认后再由开发人员修改 `RS485Utils.py`、`home.py`、Qt UI 和配置文件。

---

## 十一、T63R 外接打印机与可配置标签需求（新增）

### 11.1 设备与 SDK 范围

* “打印箱码”改为调用工位外接的 JIEPRT RFID 标签打印机，本次开发以项目根目录 `T63R-Windows-SDK-V1.3.0.0` 为厂家 SDK 基线；现场实际型号、固件版本和通信方式应在联调时记录。
* 新打印通道必须直接封装 `libDSThermal.dll` 提供的 C 接口，不再仅依赖 Windows 打印队列名称。程序至少支持 SDK 的 USB 枚举连接；如现场使用网口，同时支持 TCP 枚举或 IP:端口直连。
* 设置页应展示 SDK 实际枚举到的设备，允许操作员选择并保存连接类型、设备标识/型号或 IP:端口，不得默认使用枚举列表中的第一台设备。

### 11.2 打印内容与布局配置

打印内容和版式必须由软件参数传入，不得把标签尺寸、坐标或箱码文本写死在 SDK 适配层中。布局单位统一使用 SDK 要求的毫米（mm）。

| 配置类别 | 必须支持的内容 |
| :--- | :--- |
| 标签画布 | 宽度、高度、旋转方向（0/90/180/270）、水平偏移、垂直偏移 |
| 条码元素 | 内容字段（默认为当前箱码）、X/Y 坐标、宽度、高度；箱码条码类型为 CODE128 |
| 文本元素 | 内容字段或固定文本、X/Y 坐标、宽度、高度、字体、字号、粗体、对齐与旋转；默认包含箱码明文 |
| 可选元素 | 固定标题、生产日期/时间、图片或 Logo；未选择的内容不传入打印任务 |
| 作业参数 | 打印份数、同步等待结果、单次打印超时；打印份数不得小于 1 |

* 设置页应提供布局预览和“测试打印”，保存前校验所有元素坐标和尺寸为非负数且不超出画布；条码内容为空、画布无效或元素越界时禁止打印并指出具体字段。
* 现有 `edit_page_width=600` 和 `edit_page_height=400` 是像素画布，不得直接当作 600 mm 和 400 mm 传给 SDK。升级时应新增独立的毫米配置并执行一次明确迁移，保留旧配置用于回退。

### 11.3 箱码打印流程和结果保护

1. 初始化 SDK，枚举并连接已选设备，查询打印机就绪状态。
2. 以配置的毫米宽高创建标签上下文，将当前箱码和其他选中内容绘制到配置坐标。
3. 调用打印并等待 SDK 明确返回结果；需要多份时按配置次数执行，每份均应记录结果。
4. 只有 SDK 返回成功后才将新箱码设为“当前可用箱码”并提示打印成功。设备离线、缺纸、开盖、通信超时或 SDK 错误时必须保留原作业上下文，不得误报成功。
5. “重试”和“补打当前箱码”必须复用原箱码，不得因重试生成新箱码或重复写入箱码业务记录。
6. 无论成功或失败都要释放标签上下文；程序退出时断开设备并清理 SDK，避免重复打开设备或句柄泄漏。

### 11.4 RFID 功能边界

本次必做范围是“将箱码与选定内容打印到标签表面”。打印机虽具备 RFID 能力，但在业务未确定 EPC/USER 数据格式、编码规则、密码、重试与校验规则之前，软件必须默认禁止 RFID 写入。后续如确认需要“打印+写卡”，应以独立需求开启，不得与本次箱码打印默认绑定。

### 11.5 新增验收标准

1. 程序能枚举、选择、保存并重连指定 T63R 打印机，不会误连其他设备。
2. 可自主配置标签毫米宽高、条码/文本内容、坐标、元素尺寸和打印份数，预览与实物布局方向一致。
3. 传入当前箱码后，实物 CODE128 可被扫码枪稳定识别，条码下方明文与当前箱码完全一致。
4. 无效布局、空内容、设备离线、缺纸、开盖和超时均有明确提示，不生成虚假成功状态。
5. 重试或补打不换箱码；快速连按物理按钮不会产生并发打印。
6. 32/64 位 DLL 与应用程序位数匹配，在无开发环境的 Windows 干净主机上能完成打印，且默认不写入 RFID 数据。
