# 激光扫码装箱识别系统 — 开发设计与技术实现说明 (.md)

> **版本**：V1.1.0  
> **日期**：2026-07-10  
> **代码基线**：`Desktop_QR_Send` 当前工作目录  
> **目的**：说明装箱扫码桌面端的架构、模块职责、业务时序、数据结构、设备协议、配置、构建部署和维护注意事项，为后续开发、联调、交付与故障定位提供依据。

> [!IMPORTANT]
> **当前生产连接地址**：`ws://gya.sales.yiknet.com/webSocket/scanCode`。该地址来自项目根目录及现有发布包中的 `config.json`，路径大小写应保持不变。当前客户端未附加 Token、用户名、密码或自定义请求头，也未使用 TLS；公网部署前应与服务端确认是否升级为 `wss://` 以及相应鉴权机制。

## 项目完整软件清单与技术职责

本项目的交付内容不仅是 `Desktop_QR_Send` 源码。位于“激光扫码”根目录中的 EXE 安装程序共同组成完整的开发、运行和硬件接入环境，后续开发人员不得在未核实现场用途的情况下删除或忽略其中任何一个文件。

| 软件/目录 | 技术职责 | 提供的能力 | 与核心程序的关系 |
| :--- | :--- | :--- | :--- |
| `Desktop_QR_Send/` | **核心业务应用与源码仓库** | PySide6 全屏 UI、海康相机控制、OpenCV/pyzbar 识别、RS485、CODE128 打印、SQLite、WebSocket、日志、配置及 PyInstaller 发布包 | 直接完成扫码装箱业务；是二次开发的主要代码目录 |
| `Anaconda3-2024.10-1-Windows-x86_64.exe` | **Python/Conda 开发运行环境** | 提供 64 位 Python、Conda 环境隔离和依赖安装能力，支持运行 `main.py`、安装第三方库、调试和执行 PyInstaller | 源码开发与维护依赖；已经打包的 `装箱扫码.exe` 自带 Python 运行时，但重新开发和构建仍需要该类 Python 环境 |
| `MVS_STD_4.4.0_240913.exe` | **海康威视 MVS 4.4 SDK、运行库和设备驱动** | 提供 `MvCameraControl.dll`、GigE/USB 相机枚举、句柄管理、连续取流、参数控制、软触发、显示和图像保存 | `hikUtils/MvImport` 会直接加载 MVS DLL，是工业相机功能的直接底层依赖 |
| `USBMSER驱动.exe` | **USB 串口/RS485 设备驱动** | 使 Windows 为 USB 转串口或远程 IO 设备创建 COM 端口，保证 pyserial 能打开配置的 `COM21` 等端口 | `RS485Utils` 的直接硬件通信基础，用于接收传感器和四路物理按钮事件 |
| `jdk-8u202-windows-x64.exe` | **Java 8 配套运行环境** | 提供 Java 8 JVM、Java 命令及相关运行库，供项目配套 Java 软件、辅助工具或相关服务运行 | 当前 `Desktop_QR_Send` Python 源码没有直接调用 Java，但完整工位环境中的 Java 配套组件依赖它 |
| `JDK环境变量自动配置工具v1.4.2.23.exe` | **Java 环境变量自动配置** | 自动设置或修正 `JAVA_HOME`、`PATH` 等，确保配套组件能找到 JDK | 应在安装 JDK 后使用，属于 Java 配套环境的部署工具 |

> [!IMPORTANT]
> “没有被 Python 源码直接 import”不等于“项目交付中没有用途”。本手册将 `Desktop_QR_Send` 视为核心业务端，将 Anaconda、MVS、USBMSER、JDK 和 JDK 配置工具视为完整部署与维护环境的组成部分。后续若拆分安装包，应先确认 Java 配套组件和现场服务的实际启动方式，再决定哪些环境进入特定工位镜像。

```text
完整工位软件栈
├─ 核心业务层：Desktop_QR_Send / 装箱扫码.exe
├─ Python开发维护层：Anaconda + 项目Python依赖
├─ 视觉设备层：MVS 4.4 + 海康相机 + MvCameraControl.dll
├─ 工业通信层：USBMSER驱动 + COM端口 + RS485远程IO/实体按钮
└─ Java配套层：JDK 8 + JDK环境变量自动配置工具 + Java配套软件/服务
```

### 交接快速索引

| 后续开发人员要找的内容 | 文档位置 |
| :--- | :--- |
| 实际服务器地址、测试地址和消息格式 | 第十、十一章 |
| EXE、驱动、SDK 的用途和安装顺序 | 第八、十二章 |
| 从按钮到相机、数据库、云端的完整流程 | 第一、三、四章 |
| RS485 指令和按钮通道映射 | 第五章 |
| SQLite 表结构和数据迁移规则 | 第六章 |
| 配置项、默认值和运行状态缓存 | 第七、十三章 |
| PyInstaller 构建和发布目录问题 | 第八、十四章 |
| 二次开发入口、改造顺序和验收方法 | 第九、十五章 |

---

## 一、系统总体架构与运行流程

系统是运行于 Windows 工业主机的 PySide6 全屏桌面应用。程序同时管理 GUI、海康工业相机、二维码识别、RS485 输入、标签打印、本地 SQLite 数据库、WebSocket 连接和提示音播放。

### 1.1 技术架构

```
┌──────────────────────────────── Windows 工业主机 ────────────────────────────────┐
│                                                                                 │
│  ┌──────────────┐     Qt信号/方法调用     ┌──────────────────────────────────┐  │
│  │ PySide6 主界面│ <────────────────────> │ Home 业务编排                    │  │
│  └──────────────┘                         └───────────┬──────────────────────┘  │
│                                                     │                          │
│      ┌──────────────────┬───────────────────────────┼───────────────┐          │
│      ▼                  ▼                           ▼               ▼          │
│ ┌──────────┐      ┌───────────┐              ┌──────────┐    ┌───────────┐     │
│ │海康 MVS SDK│     │OpenCV/pyzbar│              │RS485串口 │    │Windows打印│     │
│ │相机取流   │      │图像与二维码│              │物理输入  │    │CODE128标签│     │
│ └──────────┘      └───────────┘              └──────────┘    └───────────┘     │
│                                                     │                          │
│                 ┌──────────────────┐                │                          │
│                 │ SQLite 本地数据库 │ <──────────────┘                          │
│                 └────────┬─────────┘                                           │
│                          │ 待上传记录                                           │
│                          ▼                                                      │
│                 ┌──────────────────┐       JSON/WebSocket       ┌────────────┐  │
│                 │ WebSocket客户端  │ <────────────────────────> │ 云端服务端 │  │
│                 └──────────────────┘                            └────────────┘  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 进程与线程模型

| 执行单元 | 创建位置 | 主要职责 |
| :--- | :--- | :--- |
| Qt 主线程 | `main.py` | 窗口、按钮、定时器、界面更新 |
| asyncio 事件循环线程 | `MainWindow.run_loop` | WebSocket 连接、心跳、收发消息 |
| RS485 监听线程 | `MainWindow.listen_to_rs485` | 串口读取与物理按钮动作分发 |
| 相机取流线程 | `CameraOperation.Start_grabbing` | 获取相机帧并刷新显示缓冲区 |
| 未上传数轮询线程 | `Home.autoPageData` | 每 5 秒统计待上传记录数 |
| 补传工作线程 | `UploadDataWorkerThread` | 批量读取本地记录并提交到 WebSocket 事件循环 |
| 声音播放线程 | `MainWindow.play_success/play_warning` | 异步播放成功/失败提示音 |

### 1.3 程序启动时序

1. 创建 `QApplication` 和 `MainWindow`。
2. 初始化按日轮转的文件日志及彩色控制台日志。
3. 从 `config.json` 加载配置到全局 `CONFIG_DATA`。
4. 创建 WebSocket 客户端，并在独立 asyncio 事件循环中启动连接任务。
5. 创建 `Home`，依次初始化样式、事件、海康相机、页面缓存和未上传数轮询。
6. 读取配置的串口，以 9600 波特率启动 RS485 监听。
7. 初始化提示音播放器。
8. 以 1920×1080 目标尺寸全屏显示主窗口。

---

## 二、项目目录与模块职责

```text
Desktop_QR_Send/
├── main.py                         # 程序入口、日志、WebSocket事件循环、RS485线程
├── config.json                     # 现场配置与运行状态缓存
├── example.db                      # 业务SQLite数据库
├── identifier.sqlite               # 随包资源，当前业务代码未直接访问
├── start.bat                       # Windows启动脚本
├── 装箱扫码.spec                    # PyInstaller构建配置
├── success.mp3 / warning.mp3       # 成功与失败提示音
├── page/
│   ├── home.py                     # 主业务流程、界面事件、相机/打印/录入编排
│   ├── setting.py                  # 设置窗口与设备枚举
│   ├── config.py                   # 配置读写
│   └── log.py                      # 日志窗口与导出
├── qtDesigner/
│   ├── qt_home.ui / qt_home.py     # 主窗口设计稿及生成代码
│   ├── qt_setting.ui / qt_setting.py
│   ├── qt_log.ui / qt_log.py
│   └── colored_log.py              # 控制台彩色日志格式
├── hikUtils/
│   ├── CamOperation_class.py       # 海康相机打开、取流、参数、触发和保存
│   └── MvImport/                   # 海康 MVS Python SDK 封装及常量
└── utils/
    ├── pyzbar_utils.py             # 图像预处理、二维码解码、结果去重
    ├── RS485Utils.py               # 串口协议解析与按钮动作映射
    ├── SQLite.py                   # SQLite单例及线程安全数据访问
    ├── web_socket_client.py        # WebSocket连接、心跳、重连、回执处理
    ├── upload_data_worker_thread.py# 手动补传线程
    ├── printUtils.py               # CODE128生成与Windows打印
    ├── sound_player.py             # 音频播放
    └── Utils.py                    # 盒码业务ID提取
```

---

## 三、主界面、字段与按钮交互

### 3.1 主界面布局模型

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ 扫码识别系统                         当前时间       [日志] [重启] [设置] [重置] │
├───────────────────────────────┬──────────────────────────────────────────────┤
│                               │ 手动操作                                     │
│      工业相机实时/定格画面      │ [打印箱码] [确认录入] [设备复位] [手动识别]     │
│                               ├──────────────────────────────────────────────┤
│                               │ 识别结果 [0/10盒]  □ □ □ □ □ □ □ □ □ □      │
│                               ├──────────────────────────────────────────────┤
│                               │ 数据统计：已识别箱 / 已识别盒 / 未上传数据       │
│                               │ [手动数据上传]                                │
│                               ├──────────────────────────────────────────────┤
│                               │ 装箱进度 当前箱码 [0/10捆] □ □ □ □ □ □ □ □ □ □│
│                               │                                      [计数归零]│
└───────────────────────────────┴──────────────────────────────────────────────┘
```

### 3.2 主界面按钮清单

| 控件 | 槽函数 | 前置校验 | 核心行为 |
| :--- | :--- | :--- | :--- |
| 打印箱码 | `Home.on_button_print` | 防止重复进入打印 | 生成时间戳箱码、写 `yk_case`、打印标签、更新缓存 |
| 确认录入 | `Home.force_ok_clicked` | 识别结果、箱码 | 以 `force=True` 调用录入逻辑，绕过数量阈值 |
| 设备复位 | `Home.on_button_cancel_clicked` | 无 | 停流、关相机、重新初始化、清理本次识别状态 |
| 手动识别 | `Home.on_button_again` | 必须已有箱码 | 恢复实时画面、取当前帧、调用识别流程 |
| 手动数据上传 | `Home.manualUpdateData` | WebSocket 已连接 | 创建补传线程，最多查询并发送 10000 条 |
| 计数归零 | `Home.clear_page_data` | 无 | 清零界面累计盒数和箱数并持久化 |
| 设置 | `Home.open_settings` | 无 | 以模态窗口打开设置页 |
| 日志 | `Home.show_log_dialog` | 无 | 加载最新日志并支持导出 |
| 重置 | `Home.reset_data` | 无 | 清理当前箱码、识别结果、装箱进度及相应缓存 |
| 重启 | `Home.restart_app` | 二次确认 | 使用当前 Python 可执行程序重新执行目标脚本 |

> [!WARNING]
> 当前“确认录入”始终走强制录入；重复盒码校验代码已被注释；`edit_max_xiang` 未参与自动满箱判断。这些是后续维护时需要重点确认的业务规则。

### 3.3 设置页面字段规范

| 配置键 | 页面名称 | 类型/来源 | 当前用途 |
| :--- | :--- | :--- | :--- |
| `combobox_printSelect` | 选择打印机 | 下拉框，枚举 Windows 打印机 | 指定标签输出设备 |
| `combobox_comSelect` | 选择串口 | 下拉框，枚举本机串口 | RS485 连接端口 |
| `edit_service` | 服务器地址 | 文本 | WebSocket URI |
| `edit_max_jian` | 一捆数量（盒） | 整数字符串 | 常规录入数量阈值 |
| `edit_max_xiang` | 一箱数量（捆） | 整数字符串 | 已保存，当前未用于满箱判断 |
| `edit_min_x/max_x` | X 轴起止 | 整数字符串 | 识别区域预留配置，当前主调用未传入 |
| `edit_min_y/max_y` | Y 轴起止 | 整数字符串 | 识别区域预留配置，当前主调用未传入 |
| `edit_page_width` | 纸张宽度 | 整数字符串 | 条码画布宽度 |
| `edit_page_height` | 纸张高度 | 整数字符串 | 条码画布高度 |
| `edit_page_num` | 打印数量 | 整数字符串 | 每次打印份数 |

保存设置时直接覆盖 `config.json`。当前代码没有表单类型、范围或空值校验；开发扩展时应在保存前验证整数、坐标范围、WebSocket URI 和设备选择。

---

## 四、核心业务流程实现

### 4.1 箱码生成与打印时序

```
用户/RS485通道4          Home             SQLite            printUtils       打印机
      │                   │                  │                   │              │
      │ 打印箱码           │                  │                   │              │
      ├──────────────────>│                  │                   │              │
      │                   │ 生成毫秒时间戳    │                   │              │
      │                   │ 写入 yk_case      │                   │              │
      │                   ├─────────────────>│                   │              │
      │                   │                  │                   │              │
      │                   │ print_barcode(case_code, 参数)       │              │
      │                   ├─────────────────────────────────────>│              │
      │                   │                  │                   │ Windows GDI  │
      │                   │                  │                   ├─────────────>│
      │                   │ 更新当前箱码并写 config.json          │              │
```

箱码由 `int(round(time.time() * 1000))` 生成。该方案在单机串行操作下可用，但没有设备号或随机段；若未来多机共享同一后端，应改为服务端发号、UUID/雪花 ID 或“工位号 + 时间 + 序列”以避免跨设备碰撞。

### 4.2 图像识别流程

`utils/pyzbar_utils.py::process_image` 的当前处理链如下：

1. 复制原图用于最终标框展示。
2. 未收到 ROI 参数时使用整幅图像。
3. 灰度化并保留原始灰度副本。
4. CLAHE：`clipLimit=2.0`，`tileGridSize=(8, 8)`。
5. Canny：阈值为 `0, 1000`。
6. 外轮廓检测，筛选四顶点且面积大于 1000 的轮廓。
7. 选取面积最大的四边形，透视变换至 200×200；失败则回退原图。
8. 分别对 CLAHE 图和 OTSU 二值图调用 pyzbar，仅识别 `QRCODE`。
9. 按原始字节内容集合去重。
10. 将结果写入 `Home.sacn_box_data`，标框定格，并调用录入与进度更新。

> [!CAUTION]
> `Thread(target=self_.on_button_ok_clicked(), daemon=True)` 会在创建线程之前立即调用函数，并把返回值作为 `target`。如需真正异步，应传递函数引用 `target=self_.on_button_ok_clicked`。同时，当前识别和录入逻辑会直接更新 Qt 控件，应统一回到 Qt 主线程或通过信号更新 UI。

### 4.3 录入与绑定流程

常规入口为 `Home.on_button_ok_clicked(force=False)`，强制入口将 `force=True`。

```text
识别结果为空？ ──是──> 告警并返回
      │否
当前箱码为空？ ──是──> 告警并返回
      │否
识别数 == edit_max_jian 或 force？ ──否──> “数据不符合标准”
      │是
逐盒构造绑定记录并写入 yk_box_case
      │
WebSocket 在线时同时提交异步发送
      │
播放成功音、累计盒数、追加一个捆、清除本次识别结果、更新配置与界面
```

业务 ID 通过 `extract_path_after_domain` 处理：优先提取 `.com/` 后的全部路径，其次提取 `.cn/` 后的路径，否则返回完整原字符串。

### 4.4 WebSocket 上传与回执

#### 客户端发送格式

```json
{
  "type": "message",
  "data": {
    "id": "盒码业务ID",
    "caseContent": "箱码",
    "boxContent": "完整盒码内容",
    "type": "生产装箱",
    "createTime": "日期时间字符串",
    "isLine": 0
  }
}
```

`createTime` 由 `json.dumps(..., default=str)` 转为字符串。

#### 服务端回执格式

成功回执：

```json
{"type": "echo", "data": "盒码业务ID"}
```

错误回执：

```json
{"type": "error", "data": "错误说明"}
```

收到 `echo` 后，客户端按 ID 查询 `yk_box_case`，将 `isLine` 置为 1 后插入 `yk_box_case_history`，再删除待上传表中的原记录。未收到回执的记录继续保留，供后续补传。

### 4.5 连接、心跳与重连

* `websockets.connect(uri)` 建立连接；
* 连接失败后等待 30 秒重试；
* 默认每 5 秒发送一次 WebSocket Ping；
* 接收循环异常后将状态改为断开并再次进入连接流程；
* 窗口关闭时停止相机、关闭相机设备、关闭 WebSocket 并停止 asyncio 循环。

### 4.6 手动补传

`UploadDataWorkerThread` 从 `yk_box_case` 按 `create_time` 升序查询，默认上限 10000 条；每条间隔 1 毫秒提交到主 asyncio 事件循环，并按已提交数量发射进度百分比。

当前进度代表“已提交发送任务”，不代表服务端已回执。若需要严格的传输完成进度，应基于 `echo` 回执数量计算，并对失败/超时单独统计。

---

## 五、RS485 设备通信协议

### 5.1 串口参数

| 参数 | 当前值 |
| :--- | :--- |
| 端口 | `config.json` 的 `combobox_comSelect` |
| 波特率 | 9600 |
| 超时 | 1 秒 |
| 轮询间隔 | 0.1 秒 |

### 5.2 数据帧

单条有效数据为 3 字节，转成十六进制后长度为 6：

```text
AA NN SS
│  │  └─ 状态：01=按下，其他=释放
│  └──── 通道/按钮编号
└─────── 帧头
```

串口一次返回多条时，以 ASCII 逗号（十六进制 `2C`）分隔。例如：`AA0101,AA0100` 表示通道 1 按下后释放。

### 5.3 动作映射

| 帧示例 | 解释 | Qt 动作 |
| :--- | :--- | :--- |
| `AA0101` | 通道 1 按下 | 延时 0.5 秒并点击 `button_again` |
| `AA0100` | 通道 1 释放 | 点击 `button_cancel` |
| `AA0200` | 通道 2 释放 | 点击 `button_ok` |
| `AA0301` | 通道 3 按下 | 点击 `button_again` |
| `AA0400` | 通道 4 释放 | 点击 `button_print` |

RS485 监听线程直接调用 Qt 控件的 `click()`。为避免跨线程 UI 风险，建议改为定义 Qt Signal，由串口线程发射信号，主线程连接到对应业务槽函数。

---

## 六、数据库设计

数据库文件为 `example.db`，连接配置为 `check_same_thread=False`，`Database` 使用单例元类和 `threading.Lock` 串行化访问。

### 6.1 箱码表 `yk_case`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `id` | INTEGER PK AUTOINCREMENT | 本地主键 |
| `case_code` | TEXT | 箱码 |
| `create_time` | DATETIME | SQLite 写入时间 |

### 6.2 待上传绑定表 `yk_box_case`

| 字段 | 类型 | 说明 |
| :--- | :--- | :--- |
| `id` | TEXT | 盒码业务 ID |
| `case_content` | TEXT | 箱码 |
| `box_content` | TEXT | 完整盒码内容 |
| `type` | TEXT | 业务类型，当前为“生产装箱” |
| `create_time` | DATETIME | 客户端录入时间 |
| `is_line` | INT | 当前写入值为 0，表示未完成服务端确认 |

### 6.3 已上传历史表 `yk_box_case_history`

字段与待上传表一致。服务端回执后，记录以 `is_line=1` 写入历史表，并从待上传表删除。

### 6.4 数据一致性说明

* 代码按 `id` 查询和删除，但现有 `yk_box_case` 正式表未声明主键或唯一约束；相同 ID 可能产生多条记录。
* 重复扫码校验已注释，数据库层也未强制唯一。
* 历史迁移由“插入历史 + 删除待上传”两个独立事务组成，不是单一原子事务。
* 建议后续增加唯一业务键、索引和事务迁移；在明确“一盒是否允许跨箱重装”规则后再决定唯一约束范围。

推荐索引方向：

```sql
CREATE INDEX IF NOT EXISTS idx_box_case_create_time ON yk_box_case(create_time);
CREATE INDEX IF NOT EXISTS idx_box_case_id ON yk_box_case(id);
CREATE INDEX IF NOT EXISTS idx_history_case_content ON yk_box_case_history(case_content);
```

---

## 七、配置、日志与资源文件

### 7.1 配置持久化

`page/config.py` 在进程内维护 `CONFIG_DATA` 字典。`setConfig(data)` 合并传入键后，将完整字典以 JSON 格式覆盖写入 `config.json`。

除设置字段外，还缓存：

| 键 | 含义 |
| :--- | :--- |
| `pageData` | 累计盒数、累计箱数、未上传数 |
| `caseData` | 当前箱内已录入的捆列表 |
| `caseCode` | 当前箱码 |

配置文件中包含服务器地址和生产状态，应限制普通用户随意修改，并在更新软件时避免覆盖现场文件。

### 7.2 日志

日志目录为 `logs/`，基础文件为 `app_log.log`：

* 每日午夜轮转；
* 配置保留 5 个备份；
* UTF-8 编码；
* 格式包含时间、级别、文件、函数、行号和消息；
* 日志窗口每次显示时重新加载最新日志文件并倒序展示；
* 支持导出当前列表为 `.txt`。

日志中会记录二维码内容、箱码、WebSocket JSON 和服务器 URI，属于生产追溯数据，发布和远程支持时应按企业数据安全要求保存与脱敏。

### 7.3 资源与运行目录

应用使用相对路径访问 `config.json`、`example.db`、`logs/`、音频和 UI 资源。因此启动工作目录必须是程序资源目录。PyInstaller `onedir` 发布包应保持这些文件与可执行文件的相对位置。

---

## 八、开发环境、构建与部署

### 8.1 目标环境

* 操作系统：Windows 10/11 64 位或兼容工业 Windows 环境；
* Python/Java 环境：核心桌面端为 Python/PySide6 应用，使用 Anaconda 进行源码开发与维护；完整交付环境同时使用 JDK 8 支撑 Java 配套软件或相关服务；
* 相机环境：海康威视 MVS SDK 及相机驱动；
* 串口环境：USB 转串口/远程 IO 对应驱动；
* 打印环境：Windows 已正确安装标签打印机驱动。

### 8.2 Python 依赖

项目 README 列出的主要依赖：

```text
PySide6
pyserial
pyzbar
opencv-python
Pillow
code128
numpy
pywin32
websockets
pygame
python-barcode
```

此外，pyzbar 在 Windows 环境需要可用的 ZBar 运行库，海康模块需要 MVS SDK 自带 DLL 与 Python 封装匹配。

### 8.3 本地运行

在项目根目录安装依赖、确认 MVS SDK 和设备驱动后运行：

```bash
python main.py
```

联调前确认：

1. `config.json` 是有效 JSON，数值字段可转换为整数；
2. 配置的 COM 口存在且未被占用；
3. 打印机名称与 Windows 系统显示一致；
4. WebSocket URI 可访问；
5. `example.db` 包含所需三张业务表；
6. 相机能被 MVS 客户端枚举和取流。

### 8.4 PyInstaller 构建

当前 `装箱扫码.spec` 使用 `onedir`、无控制台、UAC 管理员权限及 `logo.ico`，并打包配置、数据库、音频、页面、工具和海康 SDK Python 文件。

```bash
pyinstaller --noconfirm 装箱扫码.spec
```

构建机应安装与目标机架构一致的 MVS SDK。构建完成后需要在干净 Windows 设备上验证缺失 DLL、打印、串口、相机和音频依赖，不应仅以开发机可运行作为发布结论。

### 8.5 发布检查清单

* 备份生产 `config.json`、`example.db` 和必要日志；
* 不用空白数据库覆盖生产数据库；
* 核对服务器地址、串口号、打印机名、装箱阈值和标签尺寸；
* 验证 MVS、USB 串口和打印机驱动；
* 进行断网录入与恢复补传测试；
* 至少完成一次标准满捆、一次尾箱强制录入、一次服务端错误回执测试；
* 确认 UAC 权限和开机启动策略符合现场要求。

---

## 九、异常处理与维护注意事项

### 9.1 已知技术风险

| 风险 | 当前表现 | 建议 |
| :--- | :--- | :--- |
| Qt 跨线程调用 | RS485 和识别流程可能直接操作控件 | 使用 Signal/Slot 将 UI 更新切回主线程 |
| 识别线程写法 | 目标函数被立即调用，线程实际无任务 | 传函数引用，并明确识别/录入线程边界 |
| 强制录入暴露 | 主“确认录入”按钮直接绕过数量校验 | 将标准录入与强制录入分开，并增加权限/二次确认 |
| 满箱参数未生效 | `edit_max_xiang` 保存但未用于判断 | 用该参数控制捆数、满箱提示和自动切箱 |
| 固定 10 位显示 | 盒位和捆位均硬编码为 10 | 根据配置动态生成进度控件 |
| 重复盒码 | 代码校验被注释，表无唯一约束 | 明确业务规则后增加应用校验和数据库约束 |
| 上传进度含义不准 | 只统计发送任务提交量 | 改为按服务端回执统计成功/失败/待确认 |
| 配置缺少校验 | 空值或非数字会在运行时转换失败 | 增加表单验证、默认值和错误提示 |
| 重启目标 | `home.py` 中按当前文件路径执行 | 在源码/打包环境分别实现可靠的进程重启入口 |
| 日志敏感信息 | 记录完整码值和 WebSocket 消息 | 增加日志脱敏、级别和保留策略 |
| 数据迁移非原子 | 插历史和删待上传分两次提交 | 使用同一事务完成状态迁移 |

### 9.2 推荐测试矩阵

| 测试域 | 关键用例 |
| :--- | :--- |
| 相机 | 无设备、单设备、断开重连、连续取流、曝光变化 |
| 识别 | 0/1/10/超过10码、倾斜、反光、低对比度、重复码、非二维码 |
| 装箱 | 无箱码识别、标准满捆、尾箱强制、切箱、重置、程序重启恢复 |
| RS485 | 每个通道的按下/释放、粘包、分包、未知帧、串口断开 |
| 打印 | 打印机离线、缺纸、不同画布尺寸、1/2份、重复打印 |
| 网络 | 首次连接失败、运行中断开、自动重连、错误回执、重复回执 |
| 数据库 | 重复 ID、大批量 10000 条、断电恢复、历史迁移失败、数据库锁 |
| 日志 | 日轮转、导出、无日志目录、中文内容、敏感字段脱敏 |

### 9.3 建议的后续演进顺序

1. 修正线程调用和 Qt 主线程更新问题。
2. 区分标准录入与强制录入，并补充班组长权限或二次确认。
3. 让“一捆数量”和“一箱数量”同时驱动业务逻辑与动态 UI。
4. 增加盒码重复规则、数据库唯一性和原子迁移事务。
5. 将补传进度改为服务端回执进度，并加入失败重试上限。
6. 增加配置校验、设备状态栏和结构化错误码。
7. 建立自动化单元测试、离线协议模拟器和发布流水线。

---

## 十、联调接口约定摘要

### 10.1 客户端到服务端

* 协议：WebSocket；
* 消息编码：UTF-8 JSON；
* 外层类型：`message`；
* 每个盒码发送一条记录；
* `id` 作为回执关联字段；
* 客户端以 Ping 帧维持连接。

### 10.2 服务端到客户端

* 成功：`{"type":"echo","data":"<id>"}`；
* 失败：`{"type":"error","data":"<message>"}`；
* 服务端应保证成功回执中的 ID 与客户端发送记录一致；
* 重复回执应具备幂等性，客户端查不到待上传记录时只记录警告；
* 若服务端需要扩展错误码，应保持 `type` 和 `data` 向后兼容，或同步升级客户端解析。

> [!IMPORTANT]
> 本文记录的是当前客户端实际协议。服务端工程未包含在本项目目录内，部署前需与服务端负责人共同确认鉴权、TLS（`wss://`）、消息大小、回执时限、幂等规则和数据保留要求。

---

## 十一、现有连接地址与接口协议详解

### 11.1 项目内发现的地址清单

| 地址 | 出现位置 | 是否由正式主流程使用 | 说明 |
| :--- | :--- | :---: | :--- |
| `ws://gya.sales.yiknet.com/webSocket/scanCode` | 根目录 `config.json`、发布包 `_internal/config.json` | **是** | 当前现场/生产配置；程序启动时读取 `edit_service` 后传给 `startWS` |
| `ws://172.20.0.24/scanCode` | `qtDesigner/qt_setting.py` 的输入框提示 | 否 | 仅为 UI Tooltip 示例，不会自动连接 |
| `ws://localhost:34455/ws/websocket` | `utils/web_socket_client.py` 独立测试入口 | 否 | 只在直接运行该工具文件时使用 |
| `ws://localhost:8080` | `page/config.py` 的测试数据 | 否 | 只在直接运行配置模块测试代码时使用 |

主程序的实际调用链为：

```text
config.json.edit_service
        │
        ▼
page.config.loadConfig()
        │
        ▼
MainWindow.__init__
        │
        ├─ websocket_uri = CONFIG_DATA["edit_service"]
        │
        ▼
utils.web_socket_client.startWS(websocket_uri, heartbeat_interval=5)
        │
        ▼
WebSocketClient.connect()
        │
        └─ websockets.connect("ws://gya.sales.yiknet.com/webSocket/scanCode")
```

当前项目中**没有发现 REST/HTTP API 调用**，也没有发现服务端源码、接口签名文档、登录接口或 Token 刷新接口。客户端与云端的业务通信只有上述 WebSocket 连接。

### 11.2 WebSocket 握手约定

| 项目 | 当前客户端行为 |
| :--- | :--- |
| Scheme | `ws://`，明文 WebSocket |
| Host | `gya.sales.yiknet.com` |
| Path | `/webSocket/scanCode`，注意 `S` 和 `C` 为大写 |
| Query 参数 | 无 |
| 子协议 `Sec-WebSocket-Protocol` | 未指定 |
| Authorization | 未设置 |
| Cookie | 未设置 |
| 自定义 Header | 未设置 |
| 业务编码 | UTF-8 JSON 文本帧 |
| 心跳 | 客户端每 5 秒发送 WebSocket Ping 控制帧 |
| 首次失败重试 | 每 30 秒重试一次，无最大次数 |
| 运行中断线 | 接收循环捕获异常后重新调用 `connect()` |

由于当前使用明文 `ws://`，箱码、盒码和业务时间可能在传输链路上以明文出现。若该地址经公网访问，推荐服务端提供 `wss://`，并增加工位身份认证、证书校验和消息防重放机制。

### 11.3 客户端上行消息

客户端每识别到一个盒码发送一条独立消息，不会把一捆盒码组成数组批量发送。外层固定为：

```json
{
  "type": "message",
  "data": {
    "id": "LoY8T5yZhpaWLIM9T58=",
    "caseContent": "1776150077913",
    "boxContent": "https://g.yiknet.com/LoY8T5yZhpaWLIM9T58=",
    "type": "生产装箱",
    "createTime": "2026-07-10 14:30:25.123456",
    "isLine": 0
  }
}
```

字段规范：

| JSON 路径 | 类型 | 必填 | 产生位置 | 规则 |
| :--- | :--- | :---: | :--- | :--- |
| `type` | String | 是 | `WebSocketClient.send` | 外层固定为 `message` |
| `data.id` | String | 是 | `extract_path_after_domain` | 优先截取 `.com/` 或 `.cn/` 后的内容；都不匹配时使用完整码值 |
| `data.caseContent` | String | 是 | `Home.case` | 当前箱码，现为毫秒时间戳字符串 |
| `data.boxContent` | String | 是 | pyzbar 解码结果 | 完整二维码文本，UTF-8 解码 |
| `data.type` | String | 是 | `Home.on_button_ok_clicked` | 当前固定为 `生产装箱` |
| `data.createTime` | String | 是 | `datetime.datetime.now()` | 原值为 Python `datetime`，JSON 序列化时通过 `default=str` 转字符串 |
| `data.isLine` | Integer | 是 | 客户端 | 上行固定为 `0` |

服务端接入时建议执行以下校验：

1. 外层 `type` 必须是 `message`。
2. `data` 必须是对象，六个业务字段均存在。
3. `id`、`caseContent`、`boxContent` 不允许为空字符串。
4. `createTime` 应兼容 `YYYY-MM-DD HH:mm:ss` 和带微秒格式。
5. 同一 `id` 的重复上报必须按业务规则幂等处理，不能仅依赖单次 WebSocket 会话。
6. 只有服务端业务数据持久化成功后才返回 `echo`。

### 11.4 服务端下行消息

当前客户端只识别两种业务消息。

#### 成功回执

```json
{
  "type": "echo",
  "data": "LoY8T5yZhpaWLIM9T58="
}
```

`data` 必须是客户端上行记录的 `data.id`。客户端收到后按此 ID 查询待上传表，将查到的第一条记录写入历史表，再按 ID 删除待上传表中的记录。

#### 错误回执

```json
{
  "type": "error",
  "data": "服务端错误说明"
}
```

当前客户端只把错误写入日志，不会弹窗、重试单条数据或改变数据库状态。该记录仍保留在待上传表中，后续手动上传可能再次发送。

#### 未支持的消息

其他 `type`、二进制帧、非 JSON 文本或缺少 `type` 的消息不会形成业务处理；非 JSON 文本只记录警告。若扩展服务端消息类型，应同步修改 `message_received`，并保证旧版本客户端可以忽略新消息。

### 11.5 回执与本地数据状态机

```text
                 扫码录入
                    │
                    ▼
         yk_box_case(is_line=0)
                    │
          WebSocket发送 message
                    │
          ┌─────────┴──────────┐
          │                    │
      未收到回执           收到 error
          │                    │
          └─────────┬──────────┘
                    │ 记录继续保留
                    ▼
                等待补传

      收到 echo(id)
             │
             ├─ 查询 yk_box_case
             ├─ 内存中改 isLine=1
             ├─ 插入 yk_box_case_history
             └─ DELETE yk_box_case WHERE id=?
```

注意：如果待上传表中存在多个相同 ID，`SELECT` 只读取一条，但 `DELETE WHERE id=?` 会删除所有同 ID 记录，可能导致未全部写入历史表。因此二次开发时应先明确唯一键，并用本地主键或组合键完成精确迁移。

### 11.6 可用于联调的最小模拟服务端

以下代码是按当前客户端协议编写的**联调示例**，不属于现有生产服务端。它用于后续开发人员在隔离网络中验证客户端发送和回执流程：

```python
import asyncio
import json
from websockets.asyncio.server import serve


async def handler(websocket):
    async for raw_message in websocket:
        try:
            message = json.loads(raw_message)
            if message.get("type") != "message":
                await websocket.send(json.dumps({
                    "type": "error",
                    "data": "unsupported message type"
                }, ensure_ascii=False))
                continue

            data = message.get("data") or {}
            record_id = data.get("id")
            if not record_id:
                await websocket.send(json.dumps({
                    "type": "error",
                    "data": "missing data.id"
                }, ensure_ascii=False))
                continue

            # 正式服务端必须先完成数据库持久化和幂等校验，再返回成功回执。
            await websocket.send(json.dumps({
                "type": "echo",
                "data": record_id
            }, ensure_ascii=False))
        except json.JSONDecodeError:
            await websocket.send(json.dumps({
                "type": "error",
                "data": "invalid json"
            }, ensure_ascii=False))


async def main():
    async with serve(handler, "0.0.0.0", 34455):
        await asyncio.Future()


asyncio.run(main())
```

测试时将 `edit_service` 临时改为 `ws://127.0.0.1:34455/ws/websocket`。测试完成后恢复生产地址，严禁把本地测试地址误发到现场。

---

## 十二、随项目提供的 EXE、驱动和运行环境

### 12.1 文件用途矩阵

| 文件 | 大小约 | 架构/类型 | 在完整交付中的用途 | 用途与安装建议 |
| :--- | ---: | :--- | :---: | :--- |
| `Desktop_QR_Send/dist/装箱扫码/装箱扫码.exe` | 6.1 MB | Windows x64 GUI | **核心生产程序** | PyInstaller 启动程序，必须连同整个 `_internal` 目录分发，不能只复制单个 EXE |
| `MVS_STD_4.4.0_240913.exe` | 307 MB | Windows 安装程序 | **工业视觉环境** | 海康威视 MVS 4.4 运行环境和驱动，提供 `MvCameraControl.dll` 及相机枚举/取流能力 |
| `USBMSER驱动.exe` | 549 KB | Windows 自解压驱动 | **RS485/远程 IO 环境** | USB 转串口/远程 IO 驱动，使 Windows 生成程序可用的 COM 端口 |
| `Anaconda3-2024.10-1-Windows-x86_64.exe` | 951 MB | Windows x86_64 安装程序 | **Python 开发维护环境** | 建立 Python/Conda 环境，用于源码运行、依赖安装、调试及重新构建发布包；打包程序自带运行时不影响 Anaconda 在维护环节的实际用途 |
| `jdk-8u202-windows-x64.exe` | 212 MB | Windows x64 安装程序 | **Java 配套运行环境** | 为项目相关 Java 软件、辅助工具或服务提供 Java 8 JVM 与运行库 |
| `JDK环境变量自动配置工具v1.4.2.23.exe` | 3.1 MB | Windows x86 GUI | **Java 部署工具** | 安装 JDK 后自动配置 `JAVA_HOME`、`PATH` 等变量，使 Java 配套组件正常启动 |

不要在未知来源或生产工位上重复执行驱动安装程序。安装前应核对文件哈希、来源和现场变更窗口，并创建系统还原点或驱动备份。

### 12.2 当前文件 SHA-256

| 文件 | SHA-256 |
| :--- | :--- |
| `装箱扫码.exe` | `1ad3dd610b6e404dccd0e4ee10580e2aee094afd2b12e44cb04d835a9441e1e5` |
| `MVS_STD_4.4.0_240913.exe` | `a431e6019cee8d21db6549b7765f762bc48ea5bb4b6502d1b21cd66d0f48b9d6` |
| `USBMSER驱动.exe` | `8af8294d4469f4fc084008687b7f8f83c3f2bc9b9b5c04e0ed4ae2cd7b491f55` |
| `Anaconda3-2024.10-1-Windows-x86_64.exe` | `c1cb433e23997c84ade4ff7241b61b2f9b10a616c230da34e641e9c96dada49d` |
| `jdk-8u202-windows-x64.exe` | `b3bcdd6007e30c7b9d459b5bef766ac49f1e0b1cc92b11b19af3cf0439c3bc9f` |
| `JDK环境变量自动配置工具v1.4.2.23.exe` | `45756cbc2727f939a73aeeda98efbb62e45931ee2603e061514336476603b33f` |

哈希用于确认交接文件是否与本次文档分析的文件一致，不代表对第三方安装程序的数字签名或安全性作出保证。

### 12.3 推荐安装顺序

1. 安装 `jdk-8u202-windows-x64.exe`，再运行 JDK 环境变量自动配置工具；通过 `java -version` 验证 Java 配套环境。
2. 安装 Anaconda，创建项目专用 Python/Conda 环境，用于源码维护、依赖安装和重新打包。
3. 安装海康 MVS 4.4，并在 MVS 客户端中确认相机可枚举、可预览。
4. 安装 `USBMSER驱动.exe`，连接远程 IO/USB 串口设备，并在设备管理器确认生成 COM 端口。
5. 安装标签打印机官方驱动，打印 Windows 测试页。
6. 将整个 `dist/装箱扫码` 目录复制到固定程序目录，不要拆散 `_internal`。
7. 配置工作目录、`config.json`、数据库、日志目录及相关 Java 配套组件后进行联合调试。

### 12.4 MVS SDK 的动态库要求

`hikUtils/MvImport/MvCameraControl_class.py` 在模块导入时直接执行：

```python
WinDLL("MvCameraControl.dll", winmode=0)
```

因此 Windows 必须能通过 DLL 搜索路径找到 `MvCameraControl.dll`。通常由 MVS 安装程序写入系统环境。若出现 `FileNotFoundError`、`OSError` 或“找不到指定模块”：

1. 确认安装的是与应用同架构的 64 位 MVS 运行环境；
2. 在 MVS 客户端验证相机；
3. 检查 MVS `Applications/Win64` 或运行库目录是否进入 DLL 搜索路径；
4. 不要随意从其他版本复制单个 DLL，避免 SDK Python 头文件与 DLL ABI 不一致；
5. 重新登录或重启 Windows，使环境变量生效。

---

## 十三、现场配置文件完整说明

### 13.1 当前根目录配置

```json
{
  "combobox_printSelect": "HPRT N31",
  "combobox_comSelect": "COM21",
  "edit_service": "ws://gya.sales.yiknet.com/webSocket/scanCode",
  "edit_max_jian": "10",
  "edit_max_xiang": "10",
  "edit_min_x": "0",
  "edit_max_x": "0",
  "edit_min_y": "0",
  "edit_max_y": "0",
  "edit_page_width": "600",
  "edit_page_height": "400",
  "edit_page_num": "2",
  "pageData": {
    "label_data_box": 102830,
    "label_data_case": 0,
    "label_data_unline": 7890
  },
  "caseData": [],
  "caseCode": null
}
```

`pageData`、`caseData`、`caseCode` 是运行状态，不应作为新工位模板中的固定业务数据。制作空白配置模板时应将统计值归零、装箱列表设为空、当前箱码设为 `null`。

### 13.2 配置校验规则建议

| 键 | 建议校验 |
| :--- | :--- |
| `combobox_printSelect` | 必须与 `QPrinterInfo.availablePrinters()` 返回名称完全一致 |
| `combobox_comSelect` | 必须是当前存在的 COM 口，例如 `COM21` |
| `edit_service` | 必须以 `ws://` 或 `wss://` 开头；生产环境推荐 `wss://` |
| `edit_max_jian` | 正整数，建议 1～10；超过 10 时当前 UI 无法完整显示 |
| `edit_max_xiang` | 正整数，建议 1～10；当前代码尚未执行该阈值 |
| ROI 四字段 | 非负整数，且 `min < max`；全为 0 时表示当前未配置有效 ROI |
| `edit_page_width/height` | 正整数；当前代码实际按像素创建图像，UI 虽标注 mm，但没有单位换算 |
| `edit_page_num` | 正整数，建议限制 1～5，避免误操作连续打印 |
| `pageData` | 三个键均为非负整数 |
| `caseData` | 数组；每项当前结构为 `{"boxContents": [...]}` |
| `caseCode` | `null` 或非空字符串 |

> [!WARNING]
> 设置页面把纸张宽高标为“mm”，但 `printUtils.py` 直接把该值作为 PIL 图像像素和打印目标坐标，没有 DPI/mm 换算。因此当前 `600×400` 实际是像素画布语义。后续若要按毫米配置，应明确打印机 DPI，并执行 `pixels = mm / 25.4 * dpi` 转换。

### 13.3 配置生效时机

| 配置 | 保存后是否立即完整生效 | 原因 |
| :--- | :---: | :--- |
| 打印机、纸张、份数 | 是，下一次打印读取全局配置 | 打印函数每次调用时读取 `CONFIG_DATA` |
| 一捆数量 | 是，下一次录入校验读取 | 录入时动态读取 |
| 串口 | 否 | RS485 对象只在主窗口初始化时创建 |
| 服务器地址 | 否 | WebSocket 客户端只在主窗口初始化时创建 |
| 一箱数量 | 否/未实现 | 当前业务逻辑没有读取它完成满箱判断 |
| ROI 坐标 | 否/未实现 | 主识别调用没有把配置坐标传给 `process_image` |

修改串口或服务器地址后应重启程序。二次开发建议在设置保存后显式关闭旧连接并重建对应设备对象。

---

## 十四、发布包结构、工作目录与构建风险

### 14.1 当前发布包

当前 `dist/装箱扫码` 约 473 MB，结构为：

```text
dist/装箱扫码/
├── 装箱扫码.exe                 # 6.1 MB，Windows x64 GUI 启动器
└── _internal/                  # 约 467 MB，运行时与项目数据
    ├── python314.dll           # 当前构建使用 Python 3.14
    ├── PySide6/                # Qt 6 运行库和插件
    ├── cv2/、numpy/、websockets/
    ├── page/、utils/、hikUtils/、qtDesigner/
    ├── config.json
    ├── example.db
    ├── success.mp3、warning.mp3
    └── 其他 Python/VC/OpenSSL/SQLite DLL
```

这是 PyInstaller `onedir` 包，`装箱扫码.exe` 与 `_internal` 必须保持相对位置。

### 14.2 当前相对路径问题

源码使用以下相对路径或 `os.getcwd()`：

* `config.json`
* `example.db`
* `logs/`
* `success.mp3`
* `warning.mp3`

但 PyInstaller 把数据文件放在 `_internal`，而 EXE 位于其上一级。用户直接双击 EXE 时，当前工作目录通常是 EXE 所在目录，代码可能找不到 `_internal/config.json`、数据库和音频；随后可能使用空配置或创建新的空数据库。

当前包必须在实际 Windows 工位验证其快捷方式“起始位置”和文件读取位置。可靠修复方案是统一路径函数：

```python
from pathlib import Path
import os
import sys


def bundled_resource(relative_path: str) -> Path:
    base = Path(getattr(sys, "_MEIPASS", Path(__file__).resolve().parent))
    return base / relative_path


def writable_data_dir() -> Path:
    base = Path(os.environ.get("PROGRAMDATA", Path.home()))
    path = base / "AnhuiDigital" / "DesktopQRSend"
    path.mkdir(parents=True, exist_ok=True)
    return path
```

推荐区分：

* **只读随包资源**：UI、音频、默认配置模板，从 `sys._MEIPASS` 读取；
* **可写现场数据**：`config.json`、`example.db`、`logs` 放入 `%ProgramData%/AnhuiDigital/DesktopQRSend/`；
* 首次启动将默认模板复制到可写目录，升级时不覆盖生产数据。

### 14.3 当前构建环境线索

* 打包运行时为 Python 3.14（存在 `python314.dll`）；
* NumPy 版本为 2.4.4；
* websockets 版本为 16.0；
* 发布目录中同时残留 Python 3.8、3.12、3.14 的项目 `__pycache__` 文件；
* Spec 文件启用 `uac_admin=True`、`console=False`、`onedir` 和 UPX；
* 构建分析警告显示 `pygame` 未找到，而 `sound_player.py` 顶层导入 `pygame`；
* 发布目录未发现明显的 `pygame` 文件，也未发现 ZBar 原生动态库文件名。

`pygame` 缺失可能导致应用在导入 `utils.sound_player` 时直接启动失败；pyzbar 若没有可用 ZBar DLL，则二维码解码会在导入或运行时失败。后续打包前必须在全新虚拟环境中安装并验证这些依赖，不能只观察 PyInstaller 构建成功。

### 14.4 建议建立锁定依赖

项目目前只有 README 中的一行安装命令，没有可复现的锁定文件。建议新增并维护：

```text
requirements.txt
requirements-lock.txt
```

至少锁定：PySide6、pyserial、pyzbar、opencv-python、Pillow、code128、numpy、pywin32、websockets、pygame、PyInstaller。Python 3.14 较新，部分依赖或 PyInstaller Hook 可能尚不稳定；生产构建可优先选择经过全部硬件测试的 Python 3.12 x64，并保持构建机与测试机版本一致。

### 14.5 发布验证必须在干净 Windows 机执行

1. 不安装 Anaconda，仅安装必要驱动和 MVS，测试发布包能否独立启动。
2. 在命令行临时启动 EXE 或提供带控制台的诊断构建，捕获顶层导入异常。
3. 验证 `config.json`、数据库、日志和 MP3 的实际读写目录。
4. 验证 `MvCameraControl.dll` 搜索路径。
5. 验证 pyzbar/ZBar、pygame、Qt 平台插件和 Windows 打印模块。
6. 断开公网后完成离线录入，再恢复网络验证补传。
7. 检查 UAC 后可写目录权限，避免把业务数据写进受保护的 `Program Files`。

---

## 十五、二次开发实施指南

### 15.1 建议先建立可运行基线

后续开发人员接手后不要立即改业务逻辑，应先固定一套可复现基线：

1. 复制并只读备份当前源码、数据库和现场配置。
2. 记录 Python、MVS、相机、串口驱动、打印机驱动版本。
3. 建立独立虚拟环境，安装锁定依赖。
4. 使用模拟 WebSocket 服务端跑通无硬件启动；再逐项接入相机、串口、打印机。
5. 建立测试数据库，禁止使用 209 MB 的生产数据库做破坏性测试。
6. 为关键流程保存基准日志和测试二维码图片。

### 15.2 新增服务端或更换地址

如果只是更换 WebSocket 服务端且协议保持不变：

1. 在设置页修改 `服务器地址`；
2. 保存后重启客户端；
3. 确认日志出现“成功连接到 WebSocket 服务端”；
4. 发送一条测试记录；
5. 服务端持久化后返回 `echo`；
6. 检查记录从 `yk_box_case` 迁移到 `yk_box_case_history`。

如果服务端要求鉴权，需要扩展：

* 配置文件：增加 `device_id`、`token` 或证书路径；
* 设置页：敏感字段应掩码，Token 不应写入普通日志；
* `websockets.connect`：按所用 websockets 版本传入附加请求头或 SSL 参数；
* 服务端：绑定工位、租户和权限；
* 日志：脱敏 Authorization、Cookie 和完整盒码；
* 配置存储：使用 Windows Credential Manager 或加密存储，避免明文 JSON。

### 15.3 新增上行字段

例如增加工位号、操作员、批次号时，推荐流程：

1. 先与服务端确定字段名、类型、必填性和兼容策略。
2. 数据库增加字段并编写可回滚迁移脚本。
3. `Home.on_button_ok_clicked` 构造数据时写入新字段。
4. `WebSocketClient.send` 保持外层协议不变，由 `data` 承载新字段。
5. 历史表同步增加字段，回执迁移不得丢字段。
6. 设置页或业务页增加录入来源。
7. 服务端先兼容新旧客户端，再发布客户端。

不建议直接改变现有字段含义。例如 `id` 同时承担业务标识和回执关联键；若要引入真正的消息 ID，应新增 `messageId`，并在过渡期同时支持旧 `id` 回执。

### 15.4 扩展扫描类型

当前 pyzbar 调用只允许 `QRCODE`。如果需要识别条形码，可在明确业务规则后增加 `CODE128`、`EAN13` 等符号：

```python
symbols = [
    pyzbar.ZBarSymbol.QRCODE,
    pyzbar.ZBarSymbol.CODE128,
]
codes = decode(processed_image, symbols=symbols)
```

同时必须处理：

* 不同码制的字符集和长度；
* 一个画面混合码制的去重与优先级；
* 箱码 CODE128 被相机误识别为盒码的排除规则；
* 条码方向、最小像素宽度和补光反射；
* `type` 与服务端字段是否需要区分码制。

### 15.5 接入第二台相机

当前 `Home.initCamera` 固定选择枚举列表索引 0，且 `obj_cam_operation` 是模块级全局对象。接入多相机时应：

1. 按序列号或用户定义名称选择设备，不按枚举顺序；
2. 将相机对象改为 `Home` 实例成员或设备管理器中的字典；
3. 每台相机使用独立取流线程、缓存锁和显示区域；
4. 明确两台相机的触发来源和识别结果合并窗口；
5. 按业务唯一键合并去重，不能仅依靠单帧集合；
6. 关闭程序时逐台停止取流并销毁句柄。

### 15.6 修改装箱规则

当前业务中“捆数”和“盒数”的变量使用存在混淆：录入时以 `edit_max_jian` 校验一捆盒数，而满箱提示又把 `len(scan_case_data)` 与同一 `edit_max_jian` 比较；`edit_max_xiang` 没有使用。

建议建立明确模型：

```text
boxes_per_bundle = edit_max_jian      # 每捆盒数
bundles_per_case = edit_max_xiang     # 每箱捆数
```

然后：

* 识别录入校验使用 `boxes_per_bundle`；
* 当前箱进度使用 `bundles_per_case`；
* 满箱后增加 `label_data_case`、锁定旧箱码并要求打印新箱码；
* UI 动态创建对应数量的盒位/捆位；
* 尾箱强制录入记录 `force=true`、原因和操作员；
* 重装箱不能只清界面，需要定义是否生成解绑/冲销事件并同步服务端。

### 15.7 重构线程和事件边界

推荐将当前逻辑分为：

```text
UI层              页面显示、用户输入、Qt Signal/Slot
业务服务层         装箱状态机、校验、强制录入、切箱
设备适配层         CameraAdapter、RS485Adapter、PrinterAdapter、SoundAdapter
数据访问层         Repository、事务、数据库迁移
通信层             WebSocketTransport、消息编解码、回执跟踪
```

设备线程只产生事件，不直接点击 Qt 控件；业务层处理事件并返回状态；UI 通过主线程信号刷新。这样可以用模拟设备完成单元测试，也方便未来更换相机、扫码枪或打印机。

### 15.8 数据库迁移策略

新增字段或约束时不要直接手工修改生产库。建议引入 `schema_version` 表和逐版本迁移：

```text
V1：现有三张业务表
V2：增加本地自增主键、message_id、device_id
V3：增加上传重试次数、最后错误、最后发送时间
V4：增加装箱批次、操作员、强制录入原因
```

每次升级：备份数据库 → 检查可用空间 → 开启事务 → 执行迁移 → 校验行数与索引 → 更新版本号 → 失败回滚。209 MB 现有数据库升级前必须在副本上测量迁移时长。

### 15.9 建议新增的可观测字段

为排查断网、重复发送和设备差异，建议待上传表增加：

| 字段 | 用途 |
| :--- | :--- |
| `local_pk` | 精确定位本地记录，避免重复 ID 删除多行 |
| `message_id` | 每次业务消息的全局唯一 ID |
| `device_id` | 工位/设备身份 |
| `bundle_id` | 一次拍照形成的捆标识 |
| `retry_count` | 已发送重试次数 |
| `last_send_time` | 最近一次发送时间 |
| `last_error` | 最近错误内容或错误码 |
| `ack_time` | 服务端回执时间 |
| `force_record` | 是否强制录入 |
| `force_reason` | 强制录入原因 |

### 15.10 二次开发完成定义

每项改造只有同时满足以下条件才算完成：

1. 源码模式与 PyInstaller 发布模式均能运行。
2. 全新 Windows 测试机无需开发环境即可启动发布包。
3. 标准装箱、尾箱、重置、切箱、断网、补传、重复回执均通过。
4. 相机、RS485、打印机任一断开时程序不崩溃且给出明确提示。
5. 数据库迁移可回滚，生产数据行数核对一致。
6. 服务端成功落库后才回 `echo`，客户端按精确消息记录迁移。
7. 日志不泄露 Token、密码、Cookie 或不必要的完整业务码。
8. 发布包附版本号、变更说明、依赖清单、哈希和回退包。

---

## 十六、源码级二次开发索引

### 16.1 入口与生命周期

| 文件/函数 | 被谁调用 | 主要副作用 | 修改注意事项 |
| :--- | :--- | :--- | :--- |
| `main.py::__main__` | Windows/Python | 创建 Qt 应用并全屏显示 | 打包入口固定为 `main.py` |
| `MainWindow.__init__` | 程序入口 | 日志、配置、WebSocket、Home、RS485、音频全部在此组装 | 建议初始化所有成员为 `None` 后再进入可能失败的 `try` |
| `MainWindow.run_loop` | asyncio 线程 | 永久运行事件循环 | 关闭时需要线程安全地停止并等待线程退出 |
| `MainWindow.listen_to_rs485` | RS485 线程 | 建立串口并永久监听 | 当前连接失败会在线程中抛出，UI 只留下日志 |
| `MainWindow.closeEvent` | Qt 窗口关闭 | 停相机、关 WebSocket、停循环 | 相机或 client 初始化失败时需做空值保护 |

### 16.2 主业务函数

| 函数 | 输入 | 输出/状态变化 | 关键依赖 |
| :--- | :--- | :--- | :--- |
| `Home.pageInit` | `CONFIG_DATA` | 恢复统计、当前捆列表和箱码 | `config.json` 内容结构必须完整 |
| `Home.on_button_again` | 无 | 获取相机当前帧并进入识别 | 要求 `self.case` 非空、相机已取流 |
| `Home.capture_and_save_image` | 相机缓冲 | 调用 `process_image` | `obj_cam_operation.buf_save_image` |
| `process_image` | Home、CameraOperation、NumPy 图像 | 写 `sacn_box_data`、定格图像、触发录入 | OpenCV、pyzbar/ZBar |
| `Home.on_button_ok_clicked` | `force` | 写待上传表、发送消息、更新捆与统计 | SQLite、WebSocket、声音、配置 |
| `Home.on_button_print` | 无 | 生成箱码、写箱码表、打印、更新页面 | `printUtils`、打印机配置 |
| `Home.reset_data` | 无 | 清除当前作业上下文 | 不会删除已经写入数据库的数据 |
| `Home.periodic_task` | 无 | 每 5 秒更新未上传数 | 无限循环，无显式停止标志 |
| `Home.manualUpdateData` | 无 | 启动最多 10000 条的补传线程 | 只检查 `client.connected` |

### 16.3 相机函数

| 函数 | 作用 | 关键参数/行为 |
| :--- | :--- | :--- |
| `Home.initCamera` | 枚举 GigE/USB 相机并选索引 0 | `MV_GIGE_DEVICE | MV_USB_DEVICE` |
| `CameraOperation.Open_device` | 创建句柄并打开设备 | GigE 自动设置最佳包大小；关闭触发模式 |
| `CameraOperation.Start_grabbing` | 开始取流并创建工作线程 | 工作线程阻塞取帧，超时 1000 ms |
| `CameraOperation.Work_thread` | 复制帧缓存并显示实时/定格画面 | `buf_lock` 保护缓存复制 |
| `CameraOperation.Set_trigger_mode` | 连续/软触发切换 | 软触发源值为 7 |
| `CameraOperation.Trigger_once` | 下发 `TriggerSoftware` | 当前主识别流程没有调用它，而是读取连续流当前帧 |
| `CameraOperation.Set_parameter` | 设置帧率、曝光、增益 | 当前设置页没有暴露这些字段 |
| `CameraOperation.Close_device` | 停线程、关设备、销毁句柄 | 当前使用异步抛异常方式停止线程，建议改为退出事件并 `join` |

### 16.4 数据访问函数

| 函数 | SQL 行为 | 返回/说明 |
| :--- | :--- | :--- |
| `case_insert_data` | `INSERT yk_case` | 记录打印箱码 |
| `box_case_insert_data` | `INSERT yk_box_case` | 新增待上传绑定记录 |
| `box_case_unuploaded` | 按时间查询最早 1 条 `is_line=0` | 当前主补传流程未使用 |
| `box_case_search_by_id` | 按 ID 查询 1 条 | 回执处理使用 |
| `box_case_delete_by_id` | 按 ID 删除 | 相同 ID 会全部删除 |
| `box_case_count_unuploaded` | 统计 `is_line=0` | 主界面每 5 秒调用 |
| `box_case_search_by_time_range` | 按创建时间查询 N 条 | 手动补传使用，当前未限定 `is_line=0`，但待上传表通常只保存未确认记录 |
| `box_case_history_insert_data` | `INSERT yk_box_case_history` | 回执后保存历史 |

### 16.5 WebSocket 函数

| 函数 | 职责 | 失败行为 |
| :--- | :--- | :--- |
| `startWS` | 创建客户端、注册监听器、调度连接任务 | 返回客户端对象，不等待连接成功 |
| `WebSocketClient.connect` | 建连、通知状态、启动心跳、进入接收循环 | 首次失败每 30 秒循环重试 |
| `WebSocketClient.send` | 包装 `{type: message, data: ...}` 并发送 | 只记录异常，不把成功/失败返回给调用方 |
| `_receive_messages` | 持续读取服务端消息 | 异常后重新连接 |
| `_heartbeat` | 每 5 秒发送 Ping | 失败后结束心跳循环，接收循环负责感知断线 |
| `message_received` | 解析 `echo/error` | JSON 异常或数据库异常仅记录日志 |

### 16.6 UI 文件维护规则

* `qtDesigner/*.ui` 是 Qt Designer 源文件。
* `qtDesigner/qt_*.py` 是由 UI 转换生成的 Python 文件。
* 界面调整应优先修改 `.ui`，再使用与 PySide6 版本匹配的 `pyside6-uic` 重新生成 `.py`。
* 重新生成会覆盖手工修改，因此业务逻辑不得写入生成文件。
* 当前同时存在 `qt_home.ui` 与 `qt_home_1920_1080.ui`，实际代码导入 `qt_home.py`；发布前应明确哪一个 `.ui` 是唯一设计源。

### 16.7 版本库信息

`Desktop_QR_Send` 自身是 Git 仓库，远端为：

```text
https://gitee.com/yiknet/Desktop_QR_Send.git
```

本次分析看到的最近提交基线为 `dbc37c3`（2025-05-19，提交说明“生产调整”）。工作目录中还存在未提交的配置、数据库、日志、网络线程、上传线程、构建目录和发布目录变化。二次开发前应：

1. 先备份并区分源码变更与生产运行数据；
2. 不把生产数据库、日志、真实服务器地址和大体积构建产物直接提交到普通源码分支；
3. 为 `config.example.json` 提供无生产数据的模板；
4. 使用 `.gitignore` 排除 `build/`、`dist/`、`logs/`、`__pycache__/` 和本地 IDE 文件；
5. 发布物通过独立制品库或带校验值的发布包管理。

---

## 十七、T63R 打印 SDK 技术接入设计（新增）

### 17.1 技术基线与接入原则

* 打印硬件为外接 JIEPRT RFID 标签打印机，厂家开发包使用项目根目录 `T63R-Windows-SDK-V1.3.0.0`，其 `libDSThermal.dll` 版本为 V1.3.0.0。
* SDK 提供 `__stdcall` C 函数接口，Python 客户端可使用 `ctypes.WinDLL` 建立薄适配层。所有函数的 `argtypes/restype`、UTF-8 字符串转换、缓冲区长度、错误码转换和句柄类型必须集中在适配层，业务层不得直接散落调用 DLL。
* 64 位程序必须加载 SDK `x64` 目录的 DLL 及必需依赖，32 位程序必须加载 `Win32` 版本；位数不匹配应在启动自检时给出明确错误。
* 建议新增独立的 `PrinterService` 管理 SDK 生命周期、设备连接、配置校验和打印锁；`Home` 只传入箱码业务数据和布局配置，不处理 SDK 句柄。

### 17.2 推荐调用时序

```text
程序启动
└─ DSTP2x_Lib_Init
   └─ DSTP2x_EnumDev(1=USB / 2=TCP)
      └─ 用户选择或按已保存标识匹配设备
         └─ DSTP2x_ConnEnumeratedDev（或 DSTP2x_ConnNetworkDev）
            └─ DSTP2x_GetPrtName / DSTP2x_GetPrtSN / DSTP2x_GetPrtStatus

单次打印箱码
└─ 校验动态内容和布局（mm）
   └─ DSTP2x_GetPrtStatus
      └─ DSTP2x_SetPrnOffset（如已配置）
         └─ DSTP2x_CreateLabelContext(label_width_mm, label_height_mm)
            ├─ DSTP2x_SetLcPrnRotate
            ├─ DSTP2x_Lbl_DrawBarCode(..., 20, case_code)
            ├─ DSTP2x_Lbl_DrawText(..., case_code/固定文本/日期)
            ├─ DSTP2x_Lbl_DrawImage（如已配置）
            ├─ DSTP2x_SetLcPrnSyncType(1)
            ├─ DSTP2x_SetLcPrnTimeout(timeout_ms)
            ├─ DSTP2x_PrintLc(..., nRfidReadType=0, ...)
            └─ finally: DSTP2x_DeleteLabelContext

程序退出
└─ DSTP2x_DisconnDev
   └─ DSTP2x_Lib_Clear
```

`DSTP2x_CreateLabelContext`、`DSTP2x_Lbl_DrawBarCode`、`DSTP2x_Lbl_DrawText` 的画布、坐标和元素宽高单位均为毫米。CODE128 在 SDK 中的类型值为 `20`。`DSTP2x_PrintLc` 本次必须传入 `nRfidReadType=0`，表示箱码标签只打印、不执行 RFID 读取；同时不得调用任何 RFID 写入接口。

### 17.3 建议配置模型

新配置应使用独立的打印结构，尺寸单位显式带 `_mm`，避免与现有像素值 `edit_page_width=600` / `edit_page_height=400` 混淆。以下仅为结构示例，实际纸张尺寸和坐标必须由现场选择/测量后保存：

```json
{
  "printer_sdk": {
    "enabled": true,
    "connection": {
      "type": "usb",
      "device_id": "由 SDK 枚举后选择",
      "ip_port": ""
    },
    "label": {
      "width_mm": 100.0,
      "height_mm": 50.0,
      "rotation": 0,
      "offset_x_mm": 0.0,
      "offset_y_mm": 0.0
    },
    "elements": [
      {
        "type": "barcode",
        "source": "case_code",
        "barcode_type": "CODE128",
        "x_mm": 5.0,
        "y_mm": 5.0,
        "width_mm": 90.0,
        "height_mm": 28.0
      },
      {
        "type": "text",
        "source": "case_code",
        "x_mm": 5.0,
        "y_mm": 35.0,
        "width_mm": 90.0,
        "height_mm": 8.0,
        "font_name": "Arial",
        "font_size": 10.0,
        "bold": false,
        "align": "center",
        "rotation": 0
      }
    ],
    "copies": 2,
    "sync": true,
    "timeout_ms": 15000,
    "rfid_write_enabled": false
  }
}
```

配置校验要求：画布宽高必须大于 0；元素 X/Y 不得为负；元素宽高必须大于 0；`x + width` 和 `y + height` 不得超出画布；旋转只允许 0/90/180/270；打印份数必须大于等于 1；条码数据不得为空。

### 17.4 业务一致性与线程模型

* SDK 调用不得阻塞 Qt UI 主线程，应在单一打印工作队列中串行执行，并用 Qt Signal 将进度和结果送回主线程。
* 打印任务必须带稳定的 `job_id`、`case_code`、布局快照和份数；同一 `job_id` 重试时不得生成新箱码。
* 新箱码可以先作为“待打印”记录保存，但只有所有必要打印份数成功后才能设为当前装箱码；失败时保留“打印失败/待重试”状态，不得覆盖原当前箱码。
* 打印忙锁要同时覆盖屏幕按钮和 RS485 触发；打印结束、取消或异常时必须在 `finally` 中释放忙锁。
* 日志至少记录任务 ID、设备标识/型号、箱码（按日志安全策略脱敏）、布局版本、份数、耗时、SDK 返回码和可读错误，不记录 RFID 密码或完整敏感数据。

### 17.5 错误处理与验证矩阵

| 场景 | 预期技术行为 |
| :--- | :--- |
| SDK/DLL 缺失或位数错误 | 启动自检显示缺失项，打印功能禁用，其他装箱数据不受损 |
| 设备未枚举/已离线 | 不创建可用箱码，允许刷新设备或重连 |
| 缺纸、开盖或设备未就绪 | 翻译 SDK 状态为中文提示，保留原当前箱码和打印任务 |
| 布局越界/空内容 | 在调用 SDK 前拦截，标出具体元素和字段 |
| 打印超时或部分份数成功 | 不自动生成新码，记录已成功份数并由操作员选择继续补打 |
| 程序退出/异常 | 删除标签上下文、断开设备、清理 SDK，释放打印锁 |
| RFID | `rfid_write_enabled=false`，不调用写入接口，`PrintLc` 的 RFID 读取类型为 0 |

发布验证必须同时覆盖：USB 实机枚举与重连、实际纸张毫米尺寸、中文/数字文本、CODE128 扫码可读性、旋转与偏移、多份、缺纸/开盖、通信断开、超时、连按去重、补打不换码、程序重启后的设备/布局恢复以及无开发环境的干净 Windows 主机打包验证。
