# 激光扫码装箱识别系统 - 产品功能说明书

> **文档版本**：V1.1.0  
> **编制日期**：2026-07-10  
> **适用产品**：激光扫码装箱识别系统（装箱扫码桌面端）  
> **适用对象**：产线操作员、班组长、设备维护人员、实施与验收人员

本文档站在生产作业与现场管理的角度，说明激光扫码装箱识别系统的使用场景、功能边界、作业流程、异常处理及运行保障。文档内容以当前项目代码、现场配置和《扫码装箱.pdf》产品说明书为依据。

## 项目软件组成与各软件功能

“激光扫码”文件夹中的项目源码和 EXE 安装程序共同组成完整的交付环境，并非互相独立的无关文件。各文件分别负责桌面应用、Python 运行与维护、工业相机驱动、串口通信以及 Java 配套环境。

| 软件/目录 | 在系统中的定位 | 主要功能 | 典型使用环节 |
| :--- | :--- | :--- | :--- |
| `Desktop_QR_Send/` | **装箱扫码核心项目** | 包含桌面端源码、界面、配置、SQLite 数据库、相机控制、二维码识别、RS485、标签打印、WebSocket 上传、日志及打包后的 `装箱扫码.exe` | 日常生产运行、现场配置、故障维护和后续二次开发 |
| `Anaconda3-2024.10-1-Windows-x86_64.exe` | **Python 开发与维护环境** | 安装完整的 64 位 Python/Conda 环境，用于从源码运行 `main.py`、安装 PySide6/OpenCV/pyzbar 等依赖、调试程序及重新打包 EXE | 开发机、维护机、需要源码调试或重新发布程序的工位 |
| `MVS_STD_4.4.0_240913.exe` | **海康工业相机 SDK 与驱动** | 安装海康威视 MVS 4.4，相机枚举、打开设备、GigE/USB 取流、曝光/增益/帧率控制、图像显示和抓拍所需的 `MvCameraControl.dll` 由该环境提供 | 首次部署相机、相机驱动修复、相机参数调试 |
| `USBMSER驱动.exe` | **USB 串口通信驱动** | 使 Windows 正确识别 USB 转串口、远程 IO 或 RS485 通信设备并生成 COM 端口，供程序接收传感器和实体按钮指令 | 首次接入 RS485 硬件、设备管理器无 COM 口或串口驱动异常时 |
| `jdk-8u202-windows-x64.exe` | **Java 8 配套运行环境** | 为项目交付环境中的 Java 配套软件、辅助工具或相关服务提供 64 位 Java 8 运行能力 | 首次搭建完整工位环境、运行需要 Java 的配套组件或服务 |
| `JDK环境变量自动配置工具v1.4.2.23.exe` | **JDK 环境配置工具** | 自动配置 `JAVA_HOME`、`PATH` 等 Java 环境变量，减少人工配置错误，使依赖 JDK 的配套软件能正确找到 Java | 安装 JDK 之后执行，或 Java 配套程序提示找不到 Java 时 |

> [!IMPORTANT]
> 上述软件均属于本项目完整交付资料并有对应用途。`Desktop_QR_Send` 是直接执行扫码装箱业务的核心；MVS 和 USBMSER 分别打通相机与 RS485 硬件；Anaconda 用于源码运行、维护和重新打包；JDK 及其自动配置工具为项目配套的 Java 软件或相关服务提供运行环境。Python 主程序源码本身没有直接导入 Java 包，但这不代表 JDK 在完整交付环境中无用途。

### 软件协作关系

```text
Anaconda / Python环境 ──运行、调试、打包──> Desktop_QR_Send 核心桌面程序
                                              │
MVS 4.4 SDK/驱动 ─────工业相机取流─────────────┤
                                              ├─识别盒码、绑定箱码、打印标签、上传数据
USBMSER 串口驱动 ─────RS485按钮/传感器─────────┤
                                              │
JDK 8 + 环境配置工具 ──支撑Java配套软件/服务─────┘
```

---

## 一、系统概述与设计目标

激光扫码装箱识别系统是一套面向工业生产线末端、仓储物流和质量追溯场景的桌面端装箱作业系统。系统通过海康威视工业相机采集包装上的二维码，自动完成多码识别、盒码与箱码绑定、本地留存和云端上传，并可通过 RS485 接收光电传感器或物理按钮指令，实现拍照、录入、复位和标签打印等现场操作。

系统的主要设计目标如下：

1. **提升装箱效率**：一次拍照识别一捆中的多个盒码，减少逐盒扫码动作。
2. **保证绑定准确**：录入前校验已识别数量和当前箱码，形成盒码与箱码的明确关联。
3. **适应工业现场**：支持全屏界面、实体按钮、工业相机、标签打印机及声音提示，便于戴手套作业和盲操作。
4. **保障数据可靠**：先写入本地 SQLite 数据库，再通过 WebSocket 上传；断网时数据不丢失，恢复连接后可补传。
5. **实现全程追溯**：记录箱码、盒码、业务类型、操作时间、上传状态和运行日志，为质量追踪和故障排查提供依据。

---

## 二、系统使用与作业模型

### 1. 使用人员与操作权限

当前桌面端未实现账号登录、角色权限或 RBAC 权限控制，启动程序后即可使用主界面的作业功能。现场管理建议通过 Windows 设备账号、工位管理制度和设备物理接触权限区分以下职责：

| 人员类型 | 建议使用范围 |
| :--- | :--- |
| 产线操作员 | 打印箱码、拍照识别、确认录入、设备复位、手动上传 |
| 班组长 | 强制录入、重新装箱、计数归零、异常确认 |
| 设备维护人员 | 相机、串口、打印机、服务器地址、装箱阈值和打印参数配置；日志导出；程序重启 |

> [!IMPORTANT]
> “确认录入”按钮在当前程序中调用强制录入逻辑，可绕过“一捆盒数”校验。若现场需严格限制强制录入，应结合设备管理制度或后续版本的权限功能执行。

### 2. 盒、捆、箱的层级模型

系统按“盒码 → 捆 → 箱码”组织装箱数据：

* **盒码**：相机从产品包装二维码中识别出的原始内容。
* **捆**：一次拍照识别并成功录入的一组盒码。默认配置为每捆 10 盒，可在设置中调整。
* **箱码**：系统按当前毫秒时间戳生成的唯一标识，并以 CODE128 条码形式打印。
* **箱**：多个已录入捆与当前箱码共同组成的装箱作业上下文。界面默认展示 10 个捆进度位；现场配置中可设置“一箱数量（捆）”。

每条上传记录均以单个盒码为粒度，保存其所属箱码，从而支持通过箱码反查箱内全部盒码。

### 3. 标准作业流程

```
[启动系统并连接设备]
          │
          ▼
[打印箱码并粘贴到外箱]
          │
          ▼
[物料到位/按下识别按钮]
          │
          ▼
[工业相机抓拍并识别多个盒码]
          │
          ▼
[数量校验] ──不符合──> [提示失败，可重新识别或强制录入]
          │符合
          ▼
[盒码与当前箱码绑定并写入本地数据库]
          │
          ├──网络在线──> [WebSocket 上传并等待服务端回执]
          │
          └──网络离线──> [保留为未上传数据，网络恢复后补传]
          │
          ▼
[更新盒数、捆数和装箱进度，继续下一捆]
```

### 4. 主界面信息模型

主界面针对 1920×1080 工业显示器采用全屏布局，主要信息包括：

* 工业相机实时画面及识别后定格画面；
* 当前一捆识别进度，默认显示 `0/10盒` 至 `10/10盒`；
* 当前装箱进度，默认显示 `0/10捆` 至 `10/10捆`；
* 当前箱码状态；
* 已识别盒数、已识别箱数和未上传数据条数；
* 当前日期时间；
* 手动识别、确认录入、打印箱码、设备复位、重置、设置、日志、重启和手动数据上传等操作入口。

---

## 三、核心业务模块详情

### （一）工业相机采集与多码识别

#### 1. 工业相机接入

系统集成海康威视 MVS SDK，可枚举并打开 GigE 或 USB 工业相机。程序启动后自动选择设备列表中的第一台相机并开始连续取流，在主界面显示实时画面。

产品硬件方案采用 600 万像素 USB3.0 工业面阵相机，并配合补光灯和光电传感器完成自动抓拍。实际相机型号、焦距、曝光及安装高度以现场交付配置为准。

#### 2. 图像预处理与二维码识别

一次触发后，系统对相机当前帧执行以下处理：

1. 确定识别区域；当前主流程未传入裁剪范围，因此使用完整画面，设置页中的 X/Y 起止坐标为后续区域裁剪预留参数。
2. 转换为灰度图。
3. 使用 CLAHE 增强局部对比度。
4. 使用 Canny 边缘检测和轮廓筛选寻找四边形区域。
5. 找到有效四边形时执行透视矫正；失败时回退到原始灰度图。
6. 分别对 CLAHE 图和 OTSU 二值化图执行二维码解码。
7. 合并并按二维码内容去重，输出本次识别结果。

当前实现只解码二维码（QRCODE）；箱码标签使用 CODE128 进行打印。

#### 3. 识别结果反馈

* 在识别图像上绘制码区边框并定格显示；
* 按识别数量点亮 1～10 号盒位；
* 记录识别耗时、识别数量、二维码内容和位置；
* 识别完成后自动进入录入校验；
* 成功和失败分别播放不同提示音。

### （二）箱码生成与标签打印

#### 1. 箱码生成

点击“打印箱码”后，系统使用当前毫秒时间戳生成箱码，并将箱码及创建时间保存到本地 `yk_case` 表。

#### 2. 标签打印

系统生成 CODE128 条码图像，在条码下方打印箱码明文，并通过 Windows 打印接口发送至配置的打印机。支持配置：

* 打印机名称；
* 标签画布宽度和高度；
* 打印份数。

默认现场配置为 HPRT N31 打印机、600×400 画布、每次打印 2 份；实际值以设备当前配置为准。

#### 3. 重新打印与切箱

已有箱码时再次执行打印，系统会清空当前临时识别结果和当前装箱进度，生成并切换到新的箱码。操作前应确认上一箱数据已经完成录入，避免未完成数据被清除。

### （三）装箱校验与数据绑定

#### 1. 前置校验

录入前系统依次检查：

1. 是否已有二维码识别结果；
2. 是否已生成当前箱码；
3. 本次识别盒码数量是否等于“一捆数量（盒）”配置。

校验失败时不执行常规录入，并显示对应提示信息。

#### 2. 标准录入

每个盒码分别生成一条“生产装箱”记录，字段包括：

| 字段 | 业务含义 |
| :--- | :--- |
| `id` | 盒码业务标识；优先提取 `.com/` 或 `.cn/` 后的路径，无法匹配时使用原始内容 |
| `caseContent` | 当前箱码 |
| `boxContent` | 相机识别的完整盒码内容 |
| `type` | 固定为“生产装箱” |
| `createTime` | 录入时间 |
| `isLine` | 上传状态，`0` 表示待上传，`1` 表示已上传历史数据 |

录入成功后，本次盒码计入已识别盒数，并将本次识别作为一个“捆”加入装箱进度。

#### 3. 强制录入

尾箱、不足一捆或其他经现场负责人确认的特殊情况，可使用强制录入跳过数量阈值校验。强制录入仍要求存在识别结果和当前箱码，并执行正常的本地存储与上传流程。

### （四）RS485 硬件联动与物理按钮

系统以 9600 波特率连接配置的串口，接收格式为 `AA + 按钮编号 + 状态` 的三字节指令；多条指令可用英文逗号分隔。状态 `01` 表示按下，其他状态按释放处理。

| 通道 | 触发状态 | 当前程序动作 |
| :---: | :---: | :--- |
| 1 | 按下 | 延时 0.5 秒后拍照识别 |
| 1 | 释放 | 设备复位并重新初始化相机 |
| 2 | 释放 | 确认录入 |
| 3 | 按下 | 重新拍照识别 |
| 4 | 释放 | 打印箱码 |

产品整机可将光电传感器及四个金属按钮接入远程 IO 转 RS485 模块。具体按钮面板文字和通道接线应以现场电气图及验收配置为准。

### （五）本地存储、云端上传与断线补传

#### 1. 本地优先存储

无论网络是否在线，录入的盒码—箱码绑定数据都会先写入本地 SQLite 数据库。主界面每 5 秒统计一次待上传记录数并更新“未上传数据”。

#### 2. 实时上传

网络在线时，客户端通过 WebSocket 发送 JSON 消息。服务端返回对应数据 ID 的 `echo` 回执后，系统才会：

1. 将记录标记为已上传；
2. 写入本地历史表；
3. 从待上传表删除该记录。

这一回执机制用于避免“已发送但服务端未确认”的数据被提前删除。

#### 3. 断线与补传

WebSocket 断开后每 30 秒尝试重连，并使用 5 秒 Ping 心跳检测连接状态。离线期间数据继续保存在本地。连接恢复后，操作员可点击“手动数据上传”，按创建时间从旧到新补传，单次最多处理 10000 条，并在按钮上显示上传进度。

### （六）配置中心与运行维护

配置中心支持以下参数：

| 配置分组 | 配置项 | 说明 |
| :--- | :--- | :--- |
| 设备 | 打印机 | 从 Windows 已安装打印机中选择 |
| 设备 | 串口 | 从当前可用串口中选择，程序固定使用 9600 波特率 |
| 网络 | 服务器地址 | WebSocket 服务地址 |
| 装箱 | 一捆数量（盒） | 常规录入所需的盒码数量阈值 |
| 装箱 | 一箱数量（捆） | 现场装箱目标参数；当前主界面固定展示 10 个捆位 |
| 识别 | X/Y 起止坐标 | 预留的图像识别区域配置 |
| 打印 | 纸张宽度、纸张高度、打印份数 | 条码标签画布及份数参数 |

配置保存在本地 `config.json`，程序重启后继续使用。部分设备参数在程序启动时初始化；修改串口、服务器或相机相关配置后，建议重启程序使配置完整生效。

### （七）日志、统计与恢复操作

* **日志查看**：查看最新运行日志，包含设备连接、扫码、录入、上传、回执及异常信息。
* **日志导出**：将当前日志列表导出为文本文件，便于技术支持分析。
* **计数归零**：只清零累计的已识别盒数和已识别箱数，不删除数据库业务记录。
* **设备复位**：重启相机取流、清除本次临时识别结果并恢复实时画面。
* **重置**：清除当前箱码、本次识别结果和当前装箱进度，但不删除已落库记录。
* **重启**：经确认后重启程序，用于配置变更或设备异常恢复。

---

## 四、页面按钮与现场操作说明

| 按钮/入口 | 使用条件 | 执行结果 | 注意事项 |
| :--- | :--- | :--- | :--- |
| 打印箱码 | 已配置可用打印机 | 生成箱码、落库并打印 CODE128 标签 | 已有箱码时会切换新箱并清空当前装箱进度 |
| 手动识别 | 已有箱码、相机正常 | 抓取当前画面并识别二维码 | 无箱码时禁止识别 |
| 确认录入 | 已有箱码和识别结果 | 强制写入当前识别结果 | 当前版本会绕过数量阈值，建议由授权人员操作 |
| 设备复位 | 相机异常或需重新采集 | 重启相机并清除本次识别结果 | 不清除当前箱码和已录入捆记录 |
| 手动数据上传 | WebSocket 已连接 | 补传本地待上传记录 | 上传期间按钮锁定，最多 10000 条/次 |
| 计数归零 | 统计需要重新计数 | 清零界面累计盒数和箱数 | 不删除业务数据 |
| 重置 | 需要重新开始当前箱 | 清除当前箱码和装箱进度 | 已落库记录不回滚 |
| 设置 | 维护或实施配置 | 修改设备、网络、装箱及打印参数 | 关键设备参数修改后建议重启 |
| 日志 | 排查异常 | 查看或导出最新日志 | 日志可能含盒码和服务器通信内容，应妥善保管 |
| 重启 | 程序或设备状态异常 | 确认后重新启动应用 | 先确认当前数据已落库 |

---

## 五、异常处理与运行保障

### 1. 常见异常处理

| 异常现象 | 系统表现 | 建议处理 |
| :--- | :--- | :--- |
| 未发现相机 | 日志提示未发现设备，无实时画面 | 检查相机供电、USB/GigE 连接、MVS 驱动及 SDK 环境后复位或重启 |
| 串口连接失败 | RS485 实体按钮无响应 | 在设置中选择正确 COM 口，检查 USB 转串口驱动、接线和端口占用 |
| 无法识别二维码 | 识别数为 0 或不足阈值 | 调整标签位置、焦距、补光、相机高度；清洁镜头；重新识别 |
| 无箱码 | 系统提示先打印箱码 | 打印并粘贴箱码后再识别或录入 |
| 数量不符合标准 | 常规录入被拒绝 | 重新整理物料并识别；尾箱经负责人确认后使用强制录入 |
| 打印失败 | 无标签输出或打印异常 | 检查打印机在线状态、耗材、驱动、名称和纸张尺寸配置 |
| 服务器断开 | 数据保存在本地，未上传数增加 | 检查网络和服务器地址；连接恢复后手动补传 |
| 服务端返回错误 | 日志记录上传异常 | 导出日志，按服务端返回信息检查数据或接口服务 |

### 2. 数据安全与可靠性

1. **本地持久化**：业务数据写入 SQLite，降低断网丢数风险。
2. **服务端回执确认**：仅在收到服务端 `echo` 后迁移至历史表并删除待上传记录。
3. **线程互斥**：数据库访问使用互斥锁，降低多线程并发读写冲突。
4. **状态缓存**：当前箱码、装箱进度和页面统计写入配置文件，程序重新启动后可恢复。
5. **日志追溯**：日志按日轮转，记录模块、函数、行号和异常内容。

### 3. 现场验收建议

交付验收至少应覆盖：相机连续取流、多码识别、重复内容去重、标准数量录入、不足数量提示、强制录入、箱码打印、四路 RS485 动作、断网落库、网络恢复补传、服务端回执、程序重启状态恢复和日志导出。

> [!NOTE]
> 产品说明书中的整机外壳、12 代 i7 工业主机、BX-552P 激光反射传感器、12 路远程 IO、600 万像素相机和补光灯属于推荐/交付硬件方案；软件功能是否可用最终取决于现场实际硬件、驱动、接线及参数配置。
