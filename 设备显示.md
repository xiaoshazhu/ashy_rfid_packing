# 设备电脑启动问题处理总结

202607221434
## 一、处理结果

| 项目 | 内容 |
| --- | --- |
| 原始现象 | 设备电脑双击 `start.bat` 后进入终端，程序在导入 `PySide6.QtWidgets` 时失败，提示按任意键后退出，无法进入装箱扫码页面。 |
| 最终结果 | 修改后的文件覆盖到设备电脑后，项目已经能够正常启动并进入装箱扫码页面。 |
| 主要原因 | 设备电脑加载 `PySide6\Qt6Core.dll` 时使用了不兼容的 ICU DLL，缺少 `UCNV_TO_U_CALLBACK_SUBSTITUTE` 入口点；同时原启动脚本的中文编码在设备 `cmd.exe` 中存在解析问题。 |
| 处理原则 | 代码只读取并使用设备电脑自身安装的 Python、PySide6、Qt、Anaconda 和 MVS 环境，没有绑定开发电脑的 Python 版本或依赖路径。 |

## 二、做了什么、有什么作用、动了哪些地方

| 序号 | 我做了什么 | 它是干什么的 | 动了哪些地方 |
| --- | --- | --- | --- |
| 1 | 根据设备电脑的报错截图定位启动失败位置 | 确认故障发生在业务页面启动前的 `PySide6.QtWidgets`/Qt DLL 加载阶段，而不是相机、扫码、串口或打印业务代码中 | 分析了 `start.bat`、`main.py` 以及设备端显示的 DLL 错误信息 |
| 2 | 重写启动脚本的 Python 选择逻辑 | 启动时优先使用设备电脑已有的 `C:\Users\admin\anaconda3\python.exe`，并兼容项目虚拟环境、其他 Anaconda 安装位置及系统 Python | 修改 `Desktop_QR_Send/start.bat` |
| 3 | 增加多种 Qt 加载模式 | 针对设备环境依次尝试 `conda`、`pyside`、`system` 三种 Qt DLL 布局，避免假定开发电脑和设备电脑的 Python/PySide6 版本相同 | 修改 `Desktop_QR_Send/start.bat`；新增 `Desktop_QR_Send/runtime_bootstrap.py` |
| 4 | 增加设备运行时引导模块 | 在导入任何 PySide6 模块之前，动态定位设备电脑自己的 PySide6、Shiboken6、Qt 和 Python DLL 目录 | 新增 `Desktop_QR_Send/runtime_bootstrap.py` |
| 5 | 保持 Windows DLL 目录句柄有效 | `os.add_dll_directory()` 返回的句柄必须持续保存，否则目录会重新从 DLL 搜索路径中移除 | 在 `Desktop_QR_Send/runtime_bootstrap.py` 中集中保存 DLL 目录句柄 |
| 6 | 检查并隔离不兼容的 ICU DLL | 检查设备上的 `icuuc.dll` 是否真正导出 `UCNV_TO_U_CALLBACK_SUBSTITUTE`；拒绝不兼容版本，只预加载与 Qt6Core 匹配的 ICU DLL | 修改 `Desktop_QR_Send/runtime_bootstrap.py` 的 ICU DLL 查找、导出检查和预加载逻辑 |
| 7 | 调整 Qt、ICU、Anaconda 和 MVS 的加载顺序 | 先锁定正确的 Qt/ICU，再加入 Python、Anaconda 和 MVS 目录，防止其他软件目录中的同名 DLL 抢先被加载 | 修改 `Desktop_QR_Send/runtime_bootstrap.py` 的 DLL 注册顺序 |
| 8 | 将运行时引导接入主程序 | 保证无论通过 `start.bat` 还是直接运行 `main.py`，都在导入 `PySide6.QtWidgets` 前完成 DLL 初始化 | 修改 `Desktop_QR_Send/main.py` 文件顶部的启动初始化部分 |
| 9 | 修复异步事件循环初始化 | 使用新的 asyncio 事件循环，避免不同 Python 版本下获取默认事件循环时出现异常 | 修改 `Desktop_QR_Send/main.py` 中 WebSocket 事件循环的创建方式 |
| 10 | 修复设备命令行编码问题 | 将 `start.bat` 保存为纯 ASCII 和 Windows CRLF 换行，避免中文提示被设备电脑的 `cmd.exe` 截断后当成命令执行 | 修改 `Desktop_QR_Send/start.bat` 的提示文字、文件编码和换行格式 |
| 11 | 移除旧打包程序的错误兜底 | 不再启动旧 `dist` 后仅根据退出码显示“正常退出”，避免页面没有出现却被误判为运行成功 | 修改 `Desktop_QR_Send/start.bat`，删除旧 `dist` 自动启动分支 |
| 12 | 增加启动日志 | 启动失败时把 Python 路径、Python 版本、Qt 加载模式、PySide6 路径、Qt DLL 路径和完整异常保存下来 | `Desktop_QR_Send/runtime_bootstrap.py` 写入 `Desktop_QR_Send/logs/startup.log` |

## 三、文件变更清单

| 文件 | 变更类型 | 主要用途 |
| --- | --- | --- |
| `Desktop_QR_Send/start.bat` | 修改 | 使用设备电脑自己的 Python 环境，选择可用的 Qt 加载模式，启动当前源码，并正确显示启动结果 |
| `Desktop_QR_Send/runtime_bootstrap.py` | 新增 | 负责设备端 PySide6、Qt、ICU、Anaconda 和 MVS DLL 的动态定位、兼容性检查与加载 |
| `Desktop_QR_Send/main.py` | 修改 | 在 PySide6 导入前调用运行时引导，并修正 WebSocket 的 asyncio 事件循环初始化 |
| `设备显示.md` | 新增 | 记录本次设备启动问题的原因、处理内容、文件变更及最终结果 |

## 四、未改动范围

| 未改动内容 | 说明 |
| --- | --- |
| 相机业务逻辑 | 没有修改相机枚举、连接、采集和图像处理流程 |
| RS485 业务逻辑 | 没有修改串口连接、监听和数据处理流程 |
| 扫码与条码逻辑 | 没有修改二维码/条码识别、打印和数据上传业务 |
| 设备配置 | 没有修改设备电脑已经安装好的 Anaconda、Python、PySide6、MVS 或硬件驱动 |
| 项目配置数据 | 没有修改 `config.json`、数据库、音频和图片资源 |

## 五、设备电脑覆盖内容

| 操作 | 文件 |
| --- | --- |
| 覆盖 | `Desktop_QR_Send/start.bat` |
| 覆盖 | `Desktop_QR_Send/main.py` |
| 新增或覆盖 | `Desktop_QR_Send/runtime_bootstrap.py` |
| 启动方式 | 在设备电脑双击 `Desktop_QR_Send/start.bat` |

