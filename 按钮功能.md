# 顶部硬件按钮与传感器功能映射文档

> 最新更新：2026年7月23日
> 状态：根据现场实物面板按钮标签及系统界面已完成 1:1 映射与代码绑定。

---

## 一、实物面板顶部按钮与传感器功能映射表

实物面板顶部控件排列顺序（从左至右）：

| 实物面板标签 | 对应的硬件通道 | 触发沿 | 绑定的界面控件 | 目标业务入口 (Python 方法) | 相对代码路径 | 功能描述 |
| :--- | :---: | :---: | :--- | :--- | :--- | :--- |
| **复位** | 通道 1 | 按下 / 释放 | `button_cancel` | `Home.on_button_cancel_clicked()` | `Desktop_QR_Send/page/home.py` | 重启相机采集流，清空本次未录入的识别结果，恢复实时画面 |
| **打印箱码** | 通道 2 | 按下 / 释放 | `button_print` | `Home.on_button_print()` | `Desktop_QR_Send/page/home.py` | 自动生成新箱码、写入本地数据库并驱动打印机打码 |
| **拍照识别** | 通道 3 | 按下 / 释放 | `button_again` | `Home.on_button_again()` | `Desktop_QR_Send/page/home.py` | 抓取当前相机实时画面帧，调用二维码识别算法进行识别 |
| **重新装箱** | 通道 4 | 按下 / 释放 | `button_reset` | `Home.reset_data()` | `Desktop_QR_Send/page/home.py` | 清空当前箱码、装箱进度和临时盒码数据，重置装箱上下文 |
| **电源开关** | — | — | — | 硬件主电源切断 | — | 控制设备主电源总开关（硬件物理层面） |
| **光电传感器** | 通道 5 | 有效沿 | `button_again` | `Home.on_button_again()` | `Desktop_QR_Send/utils/RS485Utils.py` | 物料到位后触发自动拍照识别 |

---

## 二、通信协议与底层实现说明

1. **协议双模兼容**：
   - **Modbus RTU 从站轮询**：每 200ms 主动发送 `01 02 00 00 00 08 79 CC` 查询命令，解析返回的 DI 掩码（支持开关量输入模块）。
   - **AA 被动推送帧**：兼容 `AA + 通道号 + 状态 + [0x2C]` 的字节流解析。
2. **硬件使能控制**：
   - 自动开启 `DTR=True` 与 `RTS=True`，保证部分 USB 转 RS485 转换板控制脚的收发使能状态。
3. **防抖与线程安全**：
   - 具备 300ms 软件消抖冷却期，防止机械按键抖动导致重复触发。
   - 所有硬件触发动作均通过 Qt `QMetaObject.invokeMethod(..., Qt.QueuedConnection)` 安全投递至 UI 主线程。

---

## 三、关键代码文件索引

| 相对文件路径 | 关键类 / 方法 | 说明 |
| :--- | :--- | :--- |
| `Desktop_QR_Send/utils/RS485Utils.py` | `RS485Utils._handle_button_event()` | 实体按钮通道与软件功能的映射分配及防抖逻辑 |
| `Desktop_QR_Send/page/home.py` | `Home.on_button_cancel_clicked()`, `on_button_print()`, `on_button_again()`, `reset_data()` | 各顶部按钮触发的核心业务逻辑方法 |
| `Desktop_QR_Send/main.py` | `MainWindow.listen_to_rs485()` | 后台串口自适应检索与监听服务 |
| `Desktop_QR_Send/page/setting.py` | `Setting.update_com_ports()` | 设置界面串口下拉选择与智能匹配 |
