# 激光扫码装箱识别系统 - 开发任务与周期规划书

本文档针对**四按钮软件功能调整**及**网络连接上线联调**进行任务分解，并结合硬件已正常投入使用的现状，重新优化并紧凑排期，以便于快速分配开发时间和上线周期。

---

## 一、 开发调整目标

1. **四按钮功能调整**：
   - 维持现有正常运行的硬件，仅在软件层面将实体按钮功能统一映射为：“**复位、打印箱码、拍照识别、重新装箱**”。
   - 统一由“按下”动作触发，增加软件消抖和忙锁，防止生产过程中误触。
   - 优化后台 RS485 监听线程与 Qt UI 主线程的交互，避免多线程冲突风险。
2. **连接上线联调**：
   - 与云端 WebSocket 生产地址 `ws://gya.sales.yiknet.com/webSocket/scanCode` 联调上线。
   - 验证常规满捆录入自动上传、断网补传机制以及回执核销后 SQLite 数据的迁移准确性。

---

## 二、 开发任务分解与工时预估 (WBS)

由于硬件已经正常使用，无需进行硬件接线排查，核心工作完全集中在**软件业务逻辑修改与网络联调**。工时单位为：**人天 (Man-Day, MD)**。

### 阶段一：RS485 通道确认与通信层改造 (0.5 MD)
> **目标**：以最快速度确认当前硬件通道号，并重构信号分发机制。

| 任务 ID | 任务名称 | 详细描述 / 涉及文件 | 预估工时 | 配合角色 |
| :--- | :--- | :--- | :---: | :---: |
| **T1.1** | 按钮通道核对 | 通过读取程序启动后的串口交互日志，快速按下四按钮以匹配硬件通道与软件配置。 | 0.1 MD | 开发 |
| **T1.2** | 统一触发与消抖 | 修改 `RS485Utils.py`，统一修改为仅按下触发（释放忽略），添加 500ms 软件消抖，防止物理按钮弹跳连击。 | 0.2 MD | 开发 |
| **T1.3** | 后台通信线程安全化 | 将后台监听线程的 `click()` 调用重构为 Qt 信号（Signal）发射，由主线程接收并执行，消除跨线程风险。 | 0.2 MD | 开发 |

---

### 阶段二：UI 槽函数与业务逻辑重构 (0.8 MD)
> **目标**：重构主页面核心按钮逻辑，增设保护措施。

| 任务 ID | 任务名称 | 详细描述 / 涉及文件 | 预估工时 | 配合角色 |
| :--- | :--- | :--- | :---: | :---: |
| **T2.1** | UI 界面控件调整 | 在 [qt_home.ui](file:///Users/wangxun/Desktop/demo/26xm/%E6%BF%80%E5%85%89%E6%89%AB%E7%A0%81/qtDesigner/qt_home.ui) 中将“重置”按钮文字及逻辑修改为“重新装箱”，重新生成 `qt_home.py`。 | 0.2 MD | 开发 |
| **T2.2** | 打印箱码逻辑控制 | 在 [home.py](file:///Users/wangxun/Desktop/demo/26xm/%E6%BF%80%E5%85%89%E6%89%AB%E7%A0%81/page/home.py) 的 `on_button_print` 增加保护：若当前箱内已有扫码记录，则阻止或弹窗确认，防止误换箱码；支持“补打当前箱码”而不生成新码。 | 0.3 MD | 开发 |
| **T2.3** | 重新装箱二次确认 | 实体按钮触发“重新装箱”时弹出确认对话框，提示“重新装箱将清除当前扫码上下文但不会撤销已落库数据”，确认后再执行 `reset_data()`。 | 0.3 MD | 开发 |

---

### 阶段三：忙锁机制与数据库一致性优化 (0.4 MD)
> **目标**：防止快速连按误触，保障 SQLite 本地数据安全。

| 任务 ID | 任务名称 | 详细描述 / 涉及文件 | 预估工时 | 配合角色 |
| :--- | :--- | :--- | :---: | :---: |
| **T3.1** | 动作忙锁实现 | 对打印、识别和重新装箱等核心动作引入状态锁，例如在打印标签或弹出确认窗期间忽略后续 RS485 触发。 | 0.2 MD | 开发 |
| **T3.2** | 数据库事务迁移优化 | 确保在收到服务器 echo 回执后，SQLite 中向历史表的写入和从待上传表中的删除在同一事务中提交，防止断网/断电导致数据不一致。 | 0.2 MD | 开发 |

---

### 阶段四：WebSocket 生产联调与补传测试 (0.5 MD)
> **目标**：接入真实网络，测试各种网络边界状态。

| 任务 ID | 任务名称 | 详细描述 / 涉及文件 | 预估工时 | 配合角色 |
| :--- | :--- | :--- | :---: | :---: |
| **T4.1** | WebSocket 上线测试 | 将 `config.json` 切换至生产地址 `ws://gya.sales.yiknet.com/webSocket/scanCode`，验证上线连接与心跳机制。 | 0.2 MD | 开发 + 云端 |
| **T4.2** | 断网补传与核销联调 | 测试在断网状态下扫码记录本地缓存，恢复网络后执行补传，并确认 echo 回执后本地数据正确核销。 | 0.3 MD | 开发 |

---

### 阶段五：打包部署与现场回归测试 (0.8 MD)
> **目标**：发布最终版本，现场验收闭环。

| 任务 ID | 任务名称 | 详细描述 / 涉及文件 | 预估工时 | 配合角色 |
| :--- | :--- | :--- | :---: | :---: |
| **T6.1** | 生产包打包与干净环境验证 | 使用 `装箱扫码.spec` 重新编译并打包，在干净主机上验证外部依赖与驱动。 | 0.3 MD | 开发 |
| **T6.2** | 现场回归测试与交付 | 现场配合测试 5 箱以上完整装箱流程，检验物理按钮响应和打印动作，完成上线交付。 | 0.5 MD | 开发 + 现场 |

---

## 三、 推荐周期排期表 (总计 3.0 工作日)

建议的开发联调总历时为 **3.0 工作日**，开发周期可按 **4 个工作日** 进行排期（包含缓冲时间）：

* **第 1 天 (上午)**：通道匹配确认，完成 RS485 通信层改造及消抖（T1.1 - T1.3）。
* **第 1 天 (下午) ~ 第 2 天 (上午)**：UI 调整及主界面业务逻辑改造（打印状态控制、重新装箱二次确认）（T2.1 - T2.3）。
* **第 2 天 (下午)**：引入物理按钮忙状态锁，优化 SQLite 回执事务（T3.1 - T3.2）。
* **第 3 天**：WebSocket 生产联调上线，测试网络断线自动重连与离线补传，打包生成发布程序（T4.1 - T5.1）。
* **第 4 天**：现场全流程回归测试，交付上线（T5.2）。

---

## 四、 关键注意事项与风险应对

1. **重新装箱与云端冲销**：当前修改仅清除本地界面状态与缓存，不删除已落库数据。若后续业务需要冲销已上传的盒码，需等云端接口支持后扩展 `cancel_case` 协议。
2. **多线程并发冲突**：串口监听、数据补传和主界面各自在独立的线程中，修改时必须继续采用 `Database()` 单例提供的 `threading.Lock` 控制 SQLite 读写锁，防止数据库写冲突。
3. **配置文件平滑升级**：程序配置及当前箱码缓存均位于 `config.json`，在打包覆盖安装时，应注意保护生产环境原有的 `config.json` 串口和网络地址配置不被覆盖。

---

## 五、 现场最终上线签字栏

| 确认角色 | 负责项 | 签字 | 确认日期 |
| :--- | :--- | :--- | :--- |
| **软件开发人** | 确认四按钮逻辑调整、忙锁及 UI 重构完毕，网络连接和数据上传稳定。 | | |
| **云端服务端人** | 确认 WebSocket 接口生产环境接收数据正常，且能正确返回 echo 回执。 | | |
| **现场负责人** | 确认四按钮功能调整符合预期，装箱流程完整回归无误，同意上线。 | | |

---

## 六、T63R 打印 SDK 接入补充任务（新增）

> **补充目标**：将“打印箱码”接入外接 JIEPRT RFID 标签打印机，以 `T63R-Windows-SDK-V1.3.0.0` 完成设备连接、动态内容传入、毫米级布局和可靠打印结果处理。本阶段默认不写入 RFID。

| 任务 ID | 任务名称 | 详细描述 / 交付物 | 预估工时 | 配合角色 |
| :--- | :--- | :--- | :---: | :---: |
| **TP1** | SDK 适配层与生命周期 | 使用 Python `ctypes` 或等价 C ABI 封装 `libDSThermal.dll`；实现 `Lib_Init/Clear`、USB/TCP 枚举与连接、状态查询、断开连接、错误码翻译和句柄释放；根据应用位数加载对应 x64/Win32 DLL。 | 0.6 MD | 开发 |
| **TP2** | 设备选择与配置升级 | 设置页改用 SDK 枚举列表，支持选择 USB 设备或配置 TCP IP:端口；保存稳定设备标识、型号和连接类型；对现有打印队列名配置提供可回退迁移。 | 0.4 MD | 开发 + 现场 |
| **TP3** | 布局模型与参数校验 | 建立毫米单位的标签宽高、旋转、偏移、打印份数以及条码/文本元素配置；支持选择当前箱码、固定文本、日期时间等内容；校验空值、负值、越界、份数和旋转范围。 | 0.5 MD | 开发 + 业务 |
| **TP4** | 标签绘制、预览与测试打印 | 使用 `CreateLabelContext`、`Lbl_DrawBarCode`（CODE128 类型 20）、`Lbl_DrawText`等接口按配置绘制；实现预览和测试打印，保证预览与实物方向、位置和尺寸一致。 | 0.5 MD | 开发 + 现场 |
| **TP5** | 箱码流程、忙锁与异常处理 | 将 `Home.on_button_print/print_barcode` 切换到 SDK 服务；打印前查询就绪状态，打印中忙锁，成功后才激活新箱码；离线、缺纸、开盖、超时和 SDK 错误明确提示；重试/补打复用原箱码，不重复落库。 | 0.5 MD | 开发 |
| **TP6** | 打包、硬件联调与验收 | 将 SDK 动态库、配置和必需依赖加入 PyInstaller 发布物；在干净 Windows 主机上验证连接、中文文本、CODE128 可读性、多份、断线重连、缺纸/开盖、重试不换码和默认不写 RFID。 | 0.7 MD | 开发 + 现场 |

补充任务预估合计 **3.2 MD**。该工时为原有四按钮与网络联调任务之外的新增工作，建议安排 **4 个工作日**（含硬件联调缓冲）：

* **第 1 天**：TP1、TP2，完成 SDK 加载、设备枚举/连接和配置升级。
* **第 2 天**：TP3、TP4，完成内容与布局配置、预览和测试打印。
* **第 3 天**：TP5，接入箱码业务流程并覆盖失败、重试和并发边界。
* **第 4 天**：TP6，打包、干净机验证和 T63R 实机验收。

### 补充任务完成定义

* 业务负责人已确认标签实际宽高、元素内容、布局和打印份数。
* 软件能按选定设备与布局传入真实箱码，实物标签经扫码枪验证通过。
* 打印失败不会误切换箱码，补打/重试不生成新码，打印期间实体按钮不会触发重复作业。
* 发布包已包含与程序位数匹配的 SDK 依赖，干净机测试通过，且 RFID 写入保持默认关闭。
